# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

districtry Wisconsin: a single-file, dependency-light web app. Click a point in Wisconsin (or search an address) and it reports every civic district containing that point and who represents you there. It serves at **districtry.com/wi/** as a folder of the consolidated districtry repo — the first state to expand **in place** rather than as a fork (the retired template route proved this tree end-to-end on the archived `DistrictExplorer-WI` repo before the fleet consolidated; `docs/EXPANSION_GUIDE.md` §2.8 is the decision record). It ships thirty-one layers — phase 1's twelve, phase 2's seven (`docs/WI_PHASE2_PLAN.md`, delivered 2026-08-26), phase 3's ten (fully delivered 2026-08-26), and phase 4's Madison city tier and Technical College District layer (2026-08-27). **MPS School Board District** is the fleet's second elected school board and this instance's first city-scoped, coverage-gated layer: statute names Wisconsin's districted school boards exactly (MPS and Racine Unified — RUSD's unpublished geometry is gap `rusd-school-board`), the eight districts come from Milwaukee's own layer witnessed against the city's open-data shapefile, and all nine directors ship from the district's own page under a committee-list witness (`wi/scripts/build_mps_school_board_districts.py`, `mps_school_board_scraper.py`). And **Aldermanic District**, 838 city and village council districts dissolved from LTSB's coded wards on the statewide municipality key (never the county-qualified `ALDER_FIPS`, which splits cross-county cities), witnessed against the state's own BAS pre-dissolve edition key for key, with ten measurably incomplete county filings excluded on the record and alderpersons named in six big cities from their own witnessed surfaces (`wi/scripts/build_wi_aldermanic_districts.py`, `wi_alderperson_scraper.py` — whose Kenosha canvass witness caught the city GIS stale on one seat at first build, and whose Madison read survives the index page's off-by-one flat-pairing trap around the vacant seat). Thirteen ride national publishers: U.S. House, WI Senate, WI Assembly, County, the three TIGER school-district tilings (Unified / Union High / Elementary), County Subdivision, City or Village, ZIP Code (ZCTA), Post Office, and the USGS-structures Police and Fire Station nearest-3 pair. **THAT LAST PAIR OF GEOGRAPHY LAYERS WAS RENAMED AND RE-NESTED 2026-08-27**, because the app had been contradicting itself in two cards at once: Wis. Stat. 5.02(11) defines a municipality as "city, town or village", the ward card and the Elections Commission's own file both use that sense, and TIGER's Places layer carries 608 Wisconsin records — 190 cities and 418 villages and NOT ONE of the state's 1,242 towns. So a reader at the Town of Adams was told "Municipality: Town of Adams" on one card and, on another labelled Municipality, that the point was "not inside any district in this layer". The layer id stays `municipality` (it is in shipped `layers=` permalinks) and the LABEL now says **City or Village**, with the empty state saying what being outside every place MEANS rather than reading as a failed lookup — the engine's new `emptyNote` hook, since "no result" is sometimes a fact about the place. County Subdivision states each subdivision's TYPE for the same reason: it is the layer that answers everywhere (1,925 records — 1,242 towns, 452 village and 219 city records, 12 Great-Lakes water), so a city answers on both cards and the type row is what tells a reader why. Its 671 city/village records answer for 608 municipalities because 58 cross a county line and get one record per county — the same 58 the Commission's polling file labels MULTIPLE COUNTIES, two publishers agreeing — and that per-county split is the measurement behind nesting County Subdivision `subOf` County, which the reference instance's township layer has always done. Six come from state sources, and each carries a lesson: **County Board District** — all 1,590 supervisory districts in one fetch, because Wis. Stat. 5.15(4)(br)1 makes every county file its boundaries with LTSB each 15 January and 15 July and LTSB publishes the aggregate (Trempealeau's 17 come from the county's own service, because LTSB's file merges two of them; `wi/scripts/build_wi_supervisory_districts.py` carries the measurement) — supervisors are NAMED in the 33 counties whose district-keyed roster this can obtain (30 board pages + the Milwaukee and Racine county GIS layers + Taylor's dated document, 706 seats, weekly CI), and the 39 that publish none readable are the standing Data-gaps entry; **Municipal Ward** — LTSB's live 7,161-ward layer, the ballot sub-unit everything else is composed from (the card also names the ward's POLLING PLACE, from three sources in preference order: Milwaukee's own CC-BY pairing, Madison's own polling layer — the city runs the election, so a city that publishes stays first — and STATEWIDE the Wisconsin Elections Commission's own list, which the Commission sent on request after this project had spent a fortnight measuring its website's Cloudflare challenge and writing the measurement down as though it described the world. IT DESCRIBED A WEBSITE: a challenge on a site is not a refusal by the agency behind it, the same lesson Knox, Johnson and Perry taught county-side in Illinois, arriving here state-side. `wi/scripts/build_wi_polling_places.py` pairs 7,131 of LTSB's 7,161 wards, 99.58%, as 72 per-county files so a reader fetches ~9 KB rather than ~950 KB; the Commission's own coordinate places the map pin, so the engine skips the geocoder exactly where geocoding is worst — the rural "1104 COUNTY ROAD C" addresses that resolve to a road or to nothing. The file is PROVISIONAL until the Commission publishes its November edition, and all five conditions of the display contract are build gates rather than wishes: the election is named on the card, the word *provisional* is in the reader's own sentence, the pull is dated and attributed, MyVote stays the primaryLink, and THE CARD RETIRES THE PAIRING ONCE THE ELECTION DATE PASSES — a per-election list that outlives its election looks current and is not. Its build also found a shipped defect nothing else had: 26 Wisconsin wards carry a LETTER, `parseInt` was dropping it, and Wauwatosa — which has no plain wards 2, 4, 6, 7, 8, 12 or 24 at all — had seven pairs of distinct wards sharing one identifier. The 29 wards in 12 municipalities the Commission's file omits are the narrowed gap `ward-polling-places`); **Circuit Court** and **Court of Appeals District** — county unions under a double witness (Wis. Stat. 753.06 / 752.11 AND wicourts.gov agreeing), both benches scraped weekly from the court system itself; and the DPI-directory **School Location** and **Library** nearest-3 points, statewide where the reference instance's library card is city-scoped. The county card names each county's **clerk** from the Blue Book cross-gated against the clerks' own association (WEC's Cloudflare challenge is recorded, never defeated — and measured SANDBOX-SIDE on 2026-08-27 by the one-shot `wi-wec-probe.yml` run: from GitHub's runners a real browser clears it in seconds and MyVote answers plain clients, so the statewide polling/early-voting/municipal-clerk questions are queued as data hunts, not blocks) — and, since phase 4, the county's **other officers** too: sheriff, district attorney, executive/administrator (the book's own CE/CA/AC/CM code decides the elected-vs-appointed label), board chair, treasurer, clerk of circuit court, register of deeds, and coroner or medical examiner, shipped DATED to the book's April 2025 snapshot because no second publisher for those offices measures open (`wi/scripts/build_wi_county_officer_roster.py` — a layout-aware x-position parse whose chair-seats witness caught the Blue Book and the state's own supervisory aggregate disagreeing about Menominee, 7 seats against 5 districts, pinned with the card claiming neither; the book's own footnote marks the Menominee–Shawano shared prosecutorial unit, Wis. Stat. ch. 978) — except the **board chair**, the one officer who is also a supervisor: boards elect their chair each April organizational meeting, so the builder reconciles the book's chair weekly against `county-board-members.json` (a chair the county's own page marks supersedes the book's and the card says so; a book chair absent from a complete roster is withheld with the reason stated — first run measured six counties past the book, two chairs off their boards entirely) — and, since the per-county officer scrape (tranches 1-3), forty-four counties' officer rows carry office phone/e-mail/page links checked weekly against the counties' own pages (`wi_county_officer_contact_scraper.py`), whose first runs caught TEN offices the book no longer has right — two stale executives (Waukesha's, whose own page records the Blue Book's county executive died in office; Portage's, whose surfaces name Eric Olson where the book has John Pavelski), seven further turnovers, and Sawyer's administrator filled where the book's cell literally prints Vacant — the interim ships from the page, and the builder's STALE_EXEC pin withholds rather than ever regressing to the deceased name. Both chambers carry the legislature's own office data. Phase 3's next four are Milwaukee's own city tier, all on the same `milwaukeeCoverage` gate as the MPS board and all built by one script with the MPS build's two-surface machinery (`wi/scripts/build_milwaukee_city_layers.py` — the city's ArcGIS layer server-reprojected, the CKAN shapefile as the build-time area-share witness): **Police District (Milwaukee)**, seven MPD districts that revisit the safety matrix's statewide drop with the city-scoped frame Chicago's own layer uses — the captains are gap `mpd-district-leadership`, because city.milwaukee.gov's Cloudflare challenge is an access control and the card therefore links MPD's district pages and names no one (re-measured 2026-08-27 from CI by the WEC probe: the city answers plain clients there, so a weekly captains scrape is queued) — and **Neighborhood (Milwaukee)**, 190 identity-only polygons whose all-caps names ship title-cased with the raw value kept, and whose witness caught the city's two copies spelling one neighborhood apart (service MCGOVERN PARK, shapefile MC GOVERN PARK — the service spelling ships) — joined 2026-08-26 by **Milwaukee Police Squad Area**, the beat analog: 25 squads under the reference instance's exact police-beat shape (subOf the district, compact card, parent dropped to outline while squads carry the fill), whose NUMBERS encode their districts (hundreds digit) and whose builder sample-verifies that against the shipped district file rather than asserting it — and **Tax Incremental District (TID)**, the reference instance's tif-district concept under Wisconsin's own name (Wis. Stat. 66.1105): the city's 79 ACTIVE finance-overlay districts, the one city layer that does not tile the city (most of Milwaukee is honestly in no TID and the standard empty state says so), whose witness is SCOPED to the city's own STATUS flag because the CKAN shapefile keeps all 56 retired districts the live layer omits — and 15 of those carry no dissolve date, which is why the flag beats the date as the filter. MADISON JOINED THE CITY TIER 2026-08-27 (phase 4 PR 4, `wi/scripts/build_madison_city_layers.py`): the TID toggle now answers in BOTH cities — Madison's 14 under a THREE-SURFACE agreement, because the city's own layer mixes district polygons with half-mile planning buffers in one file (the HALFMILERULE flag separates the concepts; every row is status 'A', buffers included) and still draws two districts DOR's certified annual active list closed, so what ships is layer ∩ DOR with display names from the city's TIF program page, and the newest district (TID 55, Voit Farm) is active with NO published geometry — gap `madison-tid-undrawn`, pinned so the build fails the day the city draws it — and **Neighborhood Association (Madison)** is the city's registered-association fabric under its honest name: 116 Active registrations of six kinds that genuinely NEST (condo and resident associations sit wholly inside neighborhood associations), so the card collects EVERY association at the point rather than the first, each labeled with its classification and linked to its profile on the city's own site; `madisonCoverage` grounds both, the city's own 137-ward fabric dissolved to the corporate limits. And **Technical College District** (`wtcs-district`, phase 4's stretch item): DPI's own 16-district WTCS fabric, statewide and IDENTITY-ONLY BY STATUTE — the board is APPOINTED (Wis. Stat. 38.08), the card says so and names no one — shipped under five gates in `wi/scripts/build_wi_wtcs_districts.py`, including a SEAT WITNESS pinning each college's home city inside its own district and the rule that the union's one lawful interior hole is Lake Winnebago (honestly inside no district; two failed gate drafts that sampled water-bearing TIGER references are part of the record — test a tiling's integrity from its own union). Phase 3's fifth through eighth are the complete statewide **NG911 tier** — `fire-service` (1,046 dispatch areas), `law-service` (639), `ems-service` (579 — regional ambulance providers re-prove the pair key, filing under several counties' authorities and sometimes under Agency_IDs that are not county domains at all) and `psap-area` (95 answering points — which 911 center answers a call placed at the point, the fleet's first PSAP layer and a concept only a statewide instance can carry; its tiling is the one with FUTURE-dated Expire rows, which is why the builder drops expired rows by date, never by presence) from the state Office of Emergency Communications' aggregate of every county's own NG911 filing ("free and open for use by the public", refreshed roughly weekly), dissolved per agency by `wi/scripts/build_wi_ng911_service_areas.py` on the DsplayName+Agency_ID **pair** (bare names collide across counties — two unrelated Rome Fire Departments — while real agencies like Appleton Fire file under two counties' authorities), with plain `-dissolve` rather than `-dissolve2`, whose mosaic flattening silently deletes the law layer's real sheriff/PD concurrent-jurisdiction overlaps (measured: 98.750% name-set agreement before the swap, 100.000% after). These are response areas, never taxing districts and never electing bodies, so the cards name no officeholder; the five county authorities whose filings are absent or partial are re-measured inside their own provisioning polygons on every build and recorded as gaps `ng911-fire-filings` / `ng911-law-filings` / `ng911-psap-filings` / `ng911-ems-filings`, with the card explaining absence in-state instead of hiding (Langlade files no NG911 data at all). Everything beyond these is this instance's next growth per `docs/EXPANSION_GUIDE.md` (Part 3 for county-by-county and city-tier builds, §2.2.1 for the layer contract, Part 4 for a new concept); the phase-3 backlog is fully delivered, and what the state's publishers still withhold lives as the Data-gaps records rather than as queued work.

**There is no build step, no framework, and no server-side code.** The app — styles, engine, and layer modules — lives inline in `index.html`. `sw.js` is the service worker; `data/app/*.json` are runtime-fetched data files; `data/state/` is the bootstrap's state config (`build_congress_roster.py` reads its FIPS/USPS/seat count — it ships in the repo and is excluded from the Pages deploy). `sources.html` carries the generated per-layer provenance matrix; `faq.html` the ten common questions, mirrored exactly in its FAQPage JSON-LD. `history.html` is the instance's deployment history — GENERATED by the repo-root `scripts/build_history_page.py` from the worksheet's `history_page` key: the stat tiles are MEASURED from the shipped data files (so any change that moves a number regenerates the page or fails `--check` in CI, and the three weekly workflows whose outputs feed a tile regenerate it in the same run), while the changelog entries are DATED, newest-first, append-only snapshots edited only in the worksheet. The masthead's fleet-wide "Why this exists" link is deliberately unchanged — the maker's motivation and this instance's record are different questions, each with its own page.

<!-- ==== GENERATED:BEGIN metro-facts ==== -->
**Metro facts** (generated from `metro-worksheet.json` — edit the worksheet and run
`python3 scripts/generate_metro_files.py`; hand-edits here fail CI):

- Metro: Wisconsin (`wisconsin`) — https://districtry.com/wi/
- Geocoders: address Photon (Wisconsin-bounded type-ahead); unbounded Photon (whole-coverage, sibling-metro lookup); POI Nominatim (office-address pin lookup, Wisconsin-bounded, serial >=1s queue)
- Ground truth: 44.89804,-89.75782 (inside Marathon County) → county Marathon County; wi-circuit-court Marathon County Circuit Court; wi-court-of-appeals Court of Appeals District III; us-house 7; school-district-unified Marathon City School District; wi-senate 29; wi-assembly 86; county-board 35; wtcs-district Northcentral Technical College District. Negative point 47.39000,-92.97000 (off the northwest corner of Wisconsin — outside the state and every starter layer).
- Layers: 31 registered (political 7, safety 8, schools 6, geography 10); `registerLayer(` floor 19. Debug namespace `window.WisconsinExplorer`.
- Scheduled workflows: `update-wi-congress-roster.yml` (Mon 13:30 UTC); `update-wi-legislature-roster.yml` (Tue 13:30 UTC); `update-wi-county-board-roster.yml` (Thu 14:30 UTC); `update-wi-circuit-court-roster.yml` (Wed 15:30 UTC); `update-wi-court-of-appeals-roster.yml` (Wed 16:30 UTC); `update-wi-county-clerk-roster.yml` (Fri 14:30 UTC); `update-mps-school-board-roster.yml` (Mon 15:30 UTC); `update-wi-alderperson-roster.yml` (Thu 15:30 UTC); `update-mpd-captains-roster.yml` (Mon 16:30 UTC); `wi-validate-sources.yml` (1st of month 13:00 UTC).
- Source registry: `scripts/validate_sources.py` (machine-checked monthly)
<!-- ==== GENERATED:END metro-facts ==== -->

## Running & testing

```bash
# From the REPO ROOT — one server, every instance:
python3 -m http.server 8000    # then open http://localhost:8000/wi/

# Behaviour gate (real Chromium boot via Playwright) — the main test:
npm install playwright@1.56.1 && npx playwright install --with-deps chromium
BASE_URL=http://localhost:8000/wi/ node wi/scripts/smoke_test.mjs

# Static gate (run after any data/app regeneration or app edit):
python3 wi/scripts/validate_index.py wi/index.html

# Generated-region gate: per-instance facts live ONCE in wi/metro-worksheet.json;
# GENERATED regions are emitted from it. NEVER hand-edit a GENERATED region:
pip install -c scripts/requirements.txt jsonschema
python3 scripts/generate_metro_files.py            # regenerate in place (all instances)
python3 scripts/generate_metro_files.py --check    # the CI drift gate

# Engine parity: the ENGINE fences are composed from the repo-root engine/ —
# edit a block THERE and recompose, never inside an instance file:
python3 scripts/compose_app.py            # splice engine/ into every instance
python3 scripts/compose_app.py --check    # the CI drift gate
```

**Sandboxed environments (Claude Code web):** the headless browser cannot reach the Leaflet CDN. The repo root's `.claude/settings.json` SessionStart hook runs `scripts/vendor_leaflet.sh`, which vendors Leaflet into `wi/scripts/vendor/leaflet/` (gitignored); `wi/scripts/smoke_test.mjs` serves it same-origin. Production and GitHub Actions CI reach the CDN directly.

## Architecture: stable core + pluggable layer modules

The metro-agnostic engine inside `index.html` is fenced with `/* ==== ENGINE:BEGIN <name> ==== */ … ENGINE:END` markers and is **composed from the single copy under the repo root's `engine/`** by `scripts/compose_app.py` — there is no release channel and no per-fork copy to drift. **Never edit inside an ENGINE fence in this file** — edit the block under `engine/` (when the change is right for every instance) and recompose. Everything Wisconsin-specific lives in the `METRO:BEGIN config` block (worksheet-generated) and this instance's own module code.

A layer module is registered via `registerLayer({ id, group, label, overlay, query, render })`; families of similar layers use the fenced factory helpers (`registerPolygonLayer`, `registerIlgaChamber` — the generic chamber factory both WI chambers and the U.S. House card use — `registerNearestPointLayer`, `registerSchoolZone`). The modules section in `index.html` carries a commented crib of each. Two invariants pervade the code: the **stale-async guard** (`if (seq !== state.sequence) return;` after every await) and **per-layer failure isolation** (a layer's failure shows a Retry inside its own card, never breaks the others).

**Honesty rules (non-negotiable):** officeholder data is never guessed — where no verifiable roster source exists, cards link to the official body instead of inventing a name (both chamber cards degrade to the district number + the legislature's own directory on a roster miss). External strings always render through `sanitize()`/`textContent`. Roster refreshes always land as PRs for human review — the two root workflows `update-wi-congress-roster.yml` and `update-wi-legislature-roster.yml` follow the fleet pattern — never as direct commits to main.

## Data pipeline

Live layers fetch Census TIGERweb (county subdivisions, places, School layers 1/2, ZCTA by state envelope) and the USGS National Map (post offices) at runtime with the point-first `.atPoint` hook. Pre-built layers ship as same-origin `data/app/` files: `state-counties.json`, `school-districts-unified.json`, `congress-districts.json` (from the bootstrap), and `wi-senate-districts.json` / `wi-assembly-districts.json` (from `wi/scripts/build_legislative_boundaries.py` — statewide TIGERweb, mapshaper-simplified, refused unless the 2,000-random-point agreement gate passes; each carries TIGERweb's ZZ water pseudo-district). Rosters: `congress-roster.json` (congress-legislators) and `wi-{senate,assembly}-members.json` (Open States `wi.csv` via `build_wi_legislature_roster.py`, floors 31/94) — all count-guarded, all refreshed weekly by CI as reviewed PRs. `county-supervisory-districts.json` and its companion `county-board-directory.json` are OPERATOR builds, not weekly ones: re-run them after an LTSB filing window (WATCH.md). The directory is not a roster of people — it carries each county's board size, read back from the shipped geometry so the two cannot disagree, and the county's own official page for the card's footer link, because Wisconsin publishes no statewide roster of county supervisors. The PEOPLE, where a county publishes them, are `county-board-members.json` — 33 counties, 706 seats, scraped weekly from each county's own page with that county's reading direction PINNED, because the page shapes include two that are the same extraction shifted by one and a wrong guess files every supervisor under their neighbour's district. Two counties ride their own GIS layers instead and Taylor a dated document its captcha-fronted host will not let anything re-read; **and the counties recorded as publishing nothing are worth re-reading, because eleven of them were publishing all along** — nine in the 2026-08-27 re-sweep, Taylor (a page nobody here had seen), and Crawford, whose front page links its full seventeen-district roster under a nav item labelled *Government*, so only the HREF says board.

## Growing this instance

A new county or layer follows the repo's `docs/EXPANSION_GUIDE.md`. The working order that guide teaches: prove the source first (a live fetch you performed), ship the boundary and its officeholder sourcing in the same change, floor every scraped count, and record what a publisher does NOT publish in the Data gaps panel rather than guessing. When a layer ships, its row in `wi/metro-worksheet.json` (`layers[]`, with a `source` block) is what puts it on the sources page and in every gate — a layer cannot ship without a provenance row. Extend `LAYER_SIDEBAR_RANK` and `WATCH.md` in the same change.
