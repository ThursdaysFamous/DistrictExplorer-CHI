name: Update WI county board roster

# Weekly refresh of the Wisconsin county board supervisors
# (wi/data/app/county-board-members.json, read by wi/index.html's County Board
# District card). Re-scrapes the 32 counties that publish a readable
# district-keyed member list — 30 board pages plus Milwaukee's and Racine's
# own GIS layers (Milwaukee witnessed against the county's Legistar API) — and, if anything changed, opens a PR rather than committing to
# main — officeholder data should get a human look before it ships.
#
# Lafayette is the 32nd and the only one that is not a weekly re-read: its host
# answers a Cloudflare managed challenge, so its sixteen seats ship from a dated
# capture of its own board page and every run prints a NOT RE-READ line with the
# capture's age. THE LIVE PAGE IS STILL TRIED ON EVERY RUN — the day it answers,
# the log says so and the county moves to the weekly list.
#
# The scraper reads each county's page with its reading direction PINNED, and a
# county whose page has reshaped fails its own count guard and is SKIPPED for
# that run rather than yielding a shifted roster. So a drop in the county count
# here is the signal to go and re-read that county's page, not a flake — and
# the builder now refuses to write when a county that shipped before resolves
# nothing, so that signal cannot be merged as a quiet deletion.
#
# One county (Rock) is read from the Internet Archive: its own host answers 403
# to this client on every path. The ladder still asks the county live first on
# every run, refuses any capture older than the April meeting that seated the
# board, and prints which rung answered on the build log.

on:
  schedule:
    - cron: "30 14 * * 4" # Thursday 14:30 UTC
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  update-roster:
    runs-on: ubuntu-latest
    steps:
      # Pushed and PR-opened as BOT_PR_TOKEN so the resulting PR actually runs
      # CI; falls back to github.token when the secret is absent.
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BOT_PR_TOKEN || github.token }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      # The board scraper itself is stdlib; pdfplumber is for the officer
      # step below, which re-parses the Blue Book's officer tables.
      - name: Install dependencies
        run: pip install -c wi/scripts/requirements.txt pdfplumber

      # Stdlib only — the scraper fetches with urllib and the builder reads the
      # shipped geometry off disk.
      - name: Scrape the 32 counties that publish a readable member list
        run: python3 wi/scripts/wi_county_board_scraper.py

      # Refuses to write below its floors (30 counties, 674 seats), checks
      # every county's seats against the district geometry actually shipped, so
      # the two publishers have to agree before this lands, and refuses to drop
      # a county that shipped last week.
      - name: Rebuild the county board roster
        run: python3 wi/scripts/build_wi_county_board_roster.py

      # The county card's board chair is reconciled against this roster (the
      # chair is the one county officer who is also a supervisor, and boards
      # elect their chair each April organizational meeting — a cycle the
      # Blue Book's snapshot cannot see). Re-parse the book's officer tables
      # (--officers-only skips the association's 12-minute clerk crawl) and
      # rebuild the officer roster so a chair change on a county's board page
      # lands on the county card the same week.
      - name: Re-parse the Blue Book officer tables
        run: python3 wi/scripts/wi_county_clerk_scraper.py --officers-only

      # phase 4 PR 2: the counties whose own officer pages measure open are
      # re-read weekly — office phone/e-mail/link, plus a name witness that
      # supersedes the book where a county has visibly moved past it
      - name: Scrape county officer contact pages
        run: python3 wi/scripts/wi_county_officer_contact_scraper.py

      - name: Rebuild the county officer roster
        run: python3 wi/scripts/build_wi_county_officer_roster.py

      # The history page's stat tiles are MEASURED from this data — regenerate
      # it in the same run so the smoke gate's build_history_page --check stays
      # green on this PR (a tile that drifted from the data it counts fails CI)
      - name: Regenerate the history page
        run: python3 scripts/build_history_page.py

      - name: Validate app + data files
        run: python3 wi/scripts/validate_index.py wi/index.html

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet -- wi/data/app/county-board-members.json wi/data/app/wi-county-officers.json wi/history.html; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Open or update pull request
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.BOT_PR_TOKEN || github.token }}
        run: |
          # Fixed branch (not per-run): an unmerged roster PR is refreshed in
          # place instead of spawning a duplicate every week.
          branch="bot/wi-county-board-roster-update"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$branch"
          git add wi/data/app/county-board-members.json wi/data/app/wi-county-officers.json wi/history.html
          git commit -m "Update WI county board supervisors from the counties' own pages"
          git push -u origin "$branch" --force
          if [ -z "$(gh pr list --head "$branch" --state open --json number --jq '.[].number')" ]; then
            gh pr create \
              --title "Update WI county board supervisors from the counties' own pages" \
              --body "Automated weekly refresh of the County Board District card's roster, generated by \`wi/scripts/wi_county_board_scraper.py\` + \`build_wi_county_board_roster.py\` from each county's own board page. Please review the diff before merging — this changes data about real officeholders. A county missing from the diff means its page failed the scraper's count guard and was skipped; that is a page to re-read, not a flake. A \`wi-county-officers.json\` change beside it is the county card's board chair following this roster (the builder's chair reconciliation — supersede/confirm/withhold, decisions on the build log)." \
              --base main \
              --head "$branch"
          else
            echo "PR already open for $branch; force-push refreshed it."
          fi
