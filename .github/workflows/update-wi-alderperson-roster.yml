name: Update WI alderperson roster

# Weekly refresh of the aldermanic-district card's roster
# (wi/data/app/wi-alderpersons.json) — the six big cities whose routes are
# verified and witnessed:
#
#   * Milwaukee — the city's own GIS layer (flaky host, retried, with the
#     same data's CKAN shapefile as fallback), WITNESSED against the city's
#     Legistar API: a name-set mismatch fails the city loudly.
#   * Madison — each alder's own /council/districtN page (the index's flat
#     pairing is a measured off-by-one trap around the vacant seat).
#   * Green Bay — the staff directory, parsed per entry container.
#   * Kenosha — the city GIS's elected-representation layer, WITNESSED
#     against the county's certified April 2026 canvass (pdfplumber,
#     positional parse); a divergence fails unless pinned with its story.
#   * Racine and Waukesha — their own council pages, readings pinned.
#
# A city whose page reshapes fails its own gate and the whole run — read the
# failure, fix the pinned reading, never loosen a floor to get green.

on:
  schedule:
    - cron: "30 15 * * 4" # Thursday 15:30 UTC
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  update-roster:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.BOT_PR_TOKEN || github.token }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -c wi/scripts/requirements.txt pdfplumber

      - name: Scrape the six cities
        run: python3 wi/scripts/wi_alderperson_scraper.py

      - name: Rebuild the alderperson roster
        run: python3 wi/scripts/build_wi_alderperson_roster.py

      - name: Validate app + data files
        run: python3 wi/scripts/validate_index.py wi/index.html

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet -- wi/data/app/wi-alderpersons.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Open or update pull request
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.BOT_PR_TOKEN || github.token }}
        run: |
          branch="bot/wi-alderperson-roster-update"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Cut the branch from main's CURRENT tip, not from the checkout this
          # run started with. actions/checkout is shallow, these runs take tens
          # of minutes, and if main gains a .github/workflows/ change meanwhile,
          # the branch ends up differing from main THERE too — which GitHub reads
          # as the push updating a workflow and refuses for a token without
          # `workflow` scope. Measured 2026-09-02 on update-municipal-officials.
          # Re-cutting leaves this branch differing from main in its data file(s)
          # alone; the rebuilt files are carried across, and a run where main
          # touched those same files errors instead of pushing, which is right.
          git fetch --depth=1 origin main
          git checkout -B "$branch" FETCH_HEAD
          git add wi/data/app/wi-alderpersons.json
          git commit -m "Update WI alderperson roster from the six cities' own surfaces"
          git push -u origin "$branch" --force
          if [ -z "$(gh pr list --head "$branch" --state open --json number --jq '.[].number')" ]; then
            gh pr create \
              --title "Update WI alderperson roster from the six cities' own surfaces" \
              --body "Automated weekly refresh of the aldermanic-district card's roster, generated by \`wi/scripts/build_wi_alderperson_roster.py\` from \`wi_alderperson_scraper.py\` (Milwaukee GIS + Legistar witness, Madison district pages, Green Bay directory, Kenosha GIS + certified-canvass witness, Racine and Waukesha council pages). Please review the diff before merging — this changes data about real officeholders." \
              --base main \
              --head "$branch"
          else
            echo "PR already open for $branch; force-push refreshed it."
          fi
