# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Chicago District Explorer: a single-file, dependency-light web app. Click a point in Chicago (or search an address) and it reports every civic district containing that point and who represents you there — wards, county/state/federal legislative districts, police districts/beats, school zones, and more. Layers are location-aware: city layers hide outside Chicago, county layers appear only in the counties that publish them, and the statewide layers (county, township, municipality, school district, ZIP) work anywhere in Illinois. Deployed as a static site to `chidistricts.com` (see `CNAME`).

**Coverage is no longer "the Chicago collar."** It began as the seven metro counties and grew one contiguous county at a time across central and northern Illinois — north to the Wisconsin line, west to the Mississippi, and south through the Metro East to the Missouri border (**89 counties served as of 2026-08-23 — 77 through their own dispatch entries, 2 through a shipped judicial circuit, and 10 through the County card alone**; these numbers move every tranche, so read `DISPATCH_COUNTY_FIPS` and `METRO_COUNTY_FIPS` in `scripts/build_metro_outline.py` as the authority, never this sentence). Jo Daviess, the 63rd, is the first county whose boundary was **PURCHASED**: its GIS office sells data under a signed licence, and the board-district shapefile ships displayed under the county's written permission (licence #008382 + the IT/GIS Director's authorization e-mail), with the raw file retained offline rather than committed — `scripts/build_jodaviess_board_districts.py` records the whole arrangement and the component hashes. COLES, the 64th, retired a gap that was never a block: its site answers HTTP 200 and merely serves an **incomplete TLS chain** (leaf only, no intermediate), which every automated client reports as a failure and no browser notices — so "unreachable" had been recorded where "hand me the issuer" was the whole story, and the scraper now supplies that intermediate by AIA with a pinned hash rather than by disabling verification. Coles also carries the fleet's counter-example to the roster-rides-the-boundary shape: its board layer publishes an `Official` column that is a 2022 snapshot and gets **six of twelve names wrong**, so the app reads geometry from the county's service and people from the county's board page (`scripts/coles_county_board_scraper.py`). CLARK, the 65th, is the first county in the fleet built **entirely out of election returns, with no map at any stage**: its Clerk answered the standing ask with "The County Board is elected by districts. I do not have maps available", which settles the board's form and refuses the geometry in one sentence — but the districts are unions of WHOLE PRECINCTS, so the county's own certified canvasses describe them completely and the Census 2020 voting districts (matching its 23 precinct names 23/23, summing to its exact population) supply the polygons; the roster is those same canvasses' certified winners, so every card row names the election that seated the member rather than claiming a currency the returns cannot show (`scripts/build_clark_boundaries.py`, `scripts/clark_county_board_scraper.py`, and `docs/EXPANSION_GUIDE.md` §2.5.1's canvass route — whose step 4, THE JASPER TEST, is the whole difference between Clark shipping and Jasper still not). That build then found the route is not one county's trick: `il-<county>.pollresults.net` / `.accessliberty.com` is one election-results vendor serving **34 Illinois counties**, publishing each one's certified results as structured data, and sweeping its twelve unserved counties the same day shipped three more — CRAWFORD and MERCER as dissolves of their census voting districts (each walking past a standing obstacle: Crawford's layers wait on a county Mapping Committee, Mercer's only map is a 2021 scan), and MOULTRIE straight onto the County card, its board proven AT LARGE by its own returns. The other nine did not ship and that is the sweep's real yield: each now carries a MEASURED blocker instead of a guess, including both enclaves — Bureau splits sixteen precincts between districts, which closes its last route, and Christian re-precincted after 2020. Twelve counties, nine honest no's, and not one e-mail sent; EDGAR followed the same evening as the fourth, and is the first the route has carried to Clark's full two-witnesses-per-district standard — its Clerk's 2022 General canvass covers all seven districts, with 2024 and the 2026 primary re-tabulating disjoint halves. It also settles where a roster comes from: its canvasses elected a District 6 member who has since left, and only the county's own page names the appointee, so GEOMETRY COMES FROM WHATEVER PROVES THE LINES AND PEOPLE FROM WHATEVER THE COUNTY MAINTAINS AS PEOPLE (`docs/DATA_LAYER_GUIDEBOOK.md`, the two 2026-08-18 entries). FRANKLIN, the 70th, proves the route is not one vendor's either: its Clerk publishes on **platinumelectionresults.com**, a THIRD results vendor carrying eight Illinois counties — two of them still unserved (Marion, Wayne) — and its per-precinct race pages compose the board's three districts from three certified elections that agree exactly, the 2026 primary naming the districts outright where the two generals name only candidates. Two traps are recorded with it: the vendor's precinct ids group in a way that reads like a district key and is not one (Browning 1 and 3 sit in District 1, Browning 2 in District 2), and the county's members page is a GRID whose roles sit in the column BEFORE their own row's name, so a flattened read pairs every chairman with the wrong member (`scripts/build_franklin_boundaries.py`, `scripts/franklin_county_board_scraper.py`). CORRECTED 2026-08-22 — EIGHT WAS THE CURRENT ELECTION'S COUNTY LIST, NOT THE VENDOR'S CARRIAGE: that figure came off the vendor's landing page, which shows only the counties in the election being run right now, while sweeping its HISTORY endpoint by county id answers for SEVENTEEN. Six are unserved frontier counties (Alexander, Fayette, Marion, Union, Wayne, Williamson), and Union and Williamson both publish COUNTYWIDE commissioner contests, which is the County-card shape. Two SERVED counties whose rosters are blocked are carried too — ADAMS (id 54), whose seven-of-21 roster ships from exactly this route as of 2026-08-22, and DEKALB (id 66), one of the three blocked-crawler counties. TEST CARRIAGE BY CONTENT ACROSS EVERY KNOWN VENDOR, never by one vendor's published county list. These are SUMMARY canvasses — they name districts and COUNT precincts, never breaking a contest out per precinct — so the route answers ROSTERS and never composes geometry. That vendor then paid for itself twice more the same afternoon: sweeping its three UNSERVED counties shipped CLINTON as the 71st — five districts whose composition three certified elections agree on, and the tightest population spread this route has produced at 7.3% — and produced two honest no's that are worth as much. MARION is composed completely by its own returns and still cannot be built: five census voting districts under the base names CENTRALIA and SALEM span three board districts each, so no name can place them, where Clinton's one unnameable merge sits wholly inside a single district and therefore cannot move a line. WAYNE had its last open question answered — its certified 2026 primary carries a numbered District 7 contest on Fairfield 1 and Fairfield 2's own ballots — and was still not built, because the composition it confirms runs 1,863 to 3,060 people against a 2,311 ideal, a 32.4% worst deviation beyond the 30% ceiling this project's own dissolve guard enforces (`scripts/build_clinton_boundaries.py`). **Contiguity was retired as a shipping gate on 2026-08-04**: a county now joins whenever a county-keyed layer answers in it, wherever it sits — the served area CAN be disjoint and has been four times (Effingham shipped the same day as that rule, making `metro-outline.json` a MultiPolygon; **ZERO islands as of 2026-08-21 evening**, the file a single Polygon again. Massac islanded that morning — on the Ohio River at the far south of the state, about eighty miles clear of the mainland with all three of its Illinois neighbours (Johnson, Pope, Pulaski) unserved — and JOHNSON ENDED IT the same evening, bordering served Saline on the mainland side and Massac on the island's. Islands can un-island and the dissolve is recomputed, never patched: Hamilton was an island for one day until Jefferson, bordering both it and the mainland, merged it back in, Effingham was one for a week until Shelby did the same, Edwards–Wabash eleven days until White, and Massac for part of one day), and each detached county follows the first-island checklist (`docs/EXPANSION_GUIDE.md` §2.5.1). **The mainland also carries FOUR HOLES — ENCLAVES, the inverse of islands: Bureau County (17011), unserved and entirely enclosed by seven served neighbours (Henry, LaSalle, Lee, Marshall, Putnam, Stark, Whiteside); Christian County (17021), enclosed by four (Sangamon, Macon, Shelby, Montgomery) the day Shelby shipped; and — since RICHLAND shipped on 2026-08-23 — a FIVE-COUNTY hole, the first enclave in the fleet that is not a single county: CLAY, FAYETTE, JASPER, MARION and WAYNE are one contiguous unserved block that used to reach the outside through Richland, and sealing Richland sealed it; and — since VERMILION shipped later the same day — a THREE-COUNTY hole, CHAMPAIGN, FORD and PIATT, which Vermilion's join sealed the same way and which this project PREDICTED WRONG before running `--check`, on the assumption that Ford was an outlet rather than part of the block. Every one of the five is individually MEASURED shut, so it is a hole made of counties that are blocked rather than unexamined, and each carries its own OUTSIDE anchor (Flora, Vandalia, Newton, Salem, Fairfield) so the whole hole is proven unwashed rather than only its rim. THAT JOIN WAS ALSO PREDICTED WRONG BEFORE `--check` WAS RUN: the reasoning said Richland enclosed nothing, which was true of every neighbour taken one at a time and false of the block taken together — **read the ring count from `build_metro_outline.py --check`, never from a map in your head.** **Pulaski very nearly became the third on 2026-08-23 and did not, which is what settles the definition:** Alexander's join left Pulaski with no unserved Illinois neighbour at all (Alexander, Union, Johnson and Massac all serve), the same test Bureau and Christian meet — but the ring count stayed at three, because Pulaski's southern edge IS the Ohio River, the Kentucky state line. A county with no unserved neighbour is an enclave only if it is INTERIOR; one that fronts the state line is a NOTCH in the outer boundary, which the dissolve walks around (the outer ring went from 2,231 vertices to 2,644 in that one change) rather than closing over. **PULASKI THEN FILLED ITS OWN NOTCH WITHIN THE HOUR** and shipped as the 84th, which is the counterpart lesson: nothing new was found and nobody was asked — the vendor holding its answer had been swept ONE ELECTION SLUG WIDE. Re-running that sweep across ten slugs found Pulaski (id 19) and Hardin (id 20) on platinumelectionresults.com, both previously recorded as uncarried, after the Alexander build had already turned up Champaign (2020 only) and Calhoun (2022 only) there. **A vendor's carriage is PER-ELECTION: sweep several slugs before recording that it does not carry a county.** Pulaski's certified 2024 primary settles its board AT LARGE (one countywide commissioner contest per party over all eleven precincts, no district-suffixed contest anywhere, and the only 1-of-1 contests are the eleven committeeperson races), its Jasper test passes 11/11 with the population identity exact, and its precincts ship while its roster does not — the county's site is unreachable and a primary names nominees, not officeholders. The outer ring went back to 2,587 vertices and Mound City moved from OUTSIDE to INSIDE. HARDIN, THE 85TH, FOLLOWED AN HOUR LATER AND CORRECTED A BAR THIS PROJECT HAD JUST INVENTED: Pulaski's own build log said Hardin lacked "a second naming source", a standard Johnson, Perry and Pulaski were never held to — each joined on ONE certified canvass naming its precincts plus the Jasper test, and Hardin's 2026 primary prints a committeeperson contest per precinct per party, twelve contests naming all six and no seventh. Its Jasper test passes 6/6 at 3,649 and its board form now rests on two vendors a decade apart. **Holding one county to a stricter standard than three others is not caution; it keeps a county dark for no stated reason.** Two of its precincts carry an EXPLICIT LABEL rather than a derived one, each with a source: McFarlan from the census's own mixed-case county-subdivision layer (title_case renders "Mcfarlan"), and Cave In Rock from the county's returns where the census hyphenates it. Elizabethtown moved OUTSIDE→INSIDE and Golconda (Pope) took the southern frontier; POPE IS NOW THE ONLY UNSERVED COUNTY IN THE FAR SOUTH. Its record was then re-measured and found wrong in three places: popecountyil.com does not answer 503, it answers 200 with a CONTENT-FREE PLACEHOLDER (stock courthouse hero, a fake 555-555-1212 telephone number, not one county link); popeco.illinois.gov is not "the Clerk's domain" in any web sense — it has NO A RECORD, only a live MX; and the vendor routes are now proven closed rather than unexamined (pollresults returns the fabricated-county md5, accessliberty 404s, the ten-slug platinum sweep of ids 1-100 misses it, GBS and Clarity exclude it). **A MAIL-ONLY `<county>.illinois.gov` IS A DOCUMENTED EXCEPTION CLASS to the Cumberland correction** — Pope, Jasper and Marion all resolve no-A with live MX, so a no-A there means NO SITE HERE and never an unreachable clerk, and all three ask routes are intact. RICHLAND, THE 86TH (2026-08-23), IS THE FIRST COUNTY IN THE FLEET COMPOSED FROM GEOMETRY RATHER THAN FROM ELECTION RETURNS, and the blocker it retired was never a missing source — it was an unfollowed link. Its canvasses COUNT each district's precincts and never name one (the Knox shape), which its gap record had treated as decisive for three weeks; meanwhile `richlandcounty.illinois.gov` links `richlandil.wthgis.com` from its own front page, and that viewer's `Richland_IL` map publishes **`County Board Districtrs` and `Voter Precincts` as two county-authored layers** (7 and 21 features, out of the county's own `17159countyboarddistricts.tml` and `votingprecincts.tml`). Overlaid, every one of the 21 precincts sits 98.4-100% inside a single district, the largest second-district share being 0.89% — digitisation slivers between two independently drawn layers, not splits. **TWO COUNTY-PUBLISHED LAYERS CAN COMPOSE EACH OTHER EVEN WHEN NEITHER IS LABELLED WITH THE OTHER**: neither carries a district attribute on a precinct or a precinct list on a district, and the relationship is in the geometry, so before writing a county off for want of a precinct-to-district TABLE, check whether it publishes the two LAYERS. Three independent witnesses agree — the certified 2024 General counts 4, 4 and 2 precincts in districts 1, 3 and 5 and the composition gives exactly those; the seven populations sum to the county's exact 15,813 across 2,066-2,494 against a 2,259 ideal (worst 10.4%); and the county's raster precinct map agrees everywhere it is legible. **IT ALSO DISPROVED ITS OWN RECORD**, which had read nine of its 21 precincts reporting as two ISBE sub-units each as strong evidence that district lines cut through precincts. They do not, and two benign explanations were tested and BOTH failed: the county sits entirely inside one congressional, senate and house district, so it is not a legislative split, and its unit school districts split two of the nine while also splitting two precincts that are not among them. **What those sub-units are is still unknown and is not guessed at** — a sub-precinct reporting unit is evidence that SOME district line crosses a precinct and never evidence of WHICH, which is why Cumberland's reading was safe there (its board contests over-summed 6+5+3 against 12) and unsafe here. Two traps are recorded with it: the viewer's bulk feature endpoint returns a delimiter-free `<index><id><name>` run in which `Olney Precinct 11020Olney Precinct 10` parses cleanly and wrongly (the first scraper draft returned `Olney Precinct 1` three times and lost 10 and 11), and its DEFAULT layer index (`mid=1`) is not Richland's at all but Dubois County, **Indiana** — the county's own map id comes from the page's `tgisClientInit("Richland_IL")` call. Its roster is the county's own board page, scoped to the roster block because the committees section above it names people who do not sit on the board, and five of the seven members are independently confirmed by certified returns (`scripts/build_richland_boundaries.py`, `scripts/richland_county_board_scraper.py`). The join sealed the five-county Clay/Fayette/Jasper/Marion/Wayne enclave described above and left Lawrence a state-line notch. JACKSON, THE 87TH (2026-08-23), IS THE FIRST COUNTY IN THE FLEET WHOSE DISTRICTS **SPLIT PRECINCTS** AND ARE BUILT ANYWAY. Its own certified 2024 General canvass reports three of its 56 precincts TWICE, under two districts each with their registration divided — Carbondale 21 (D6 600 / D7 282), Carbondale 24 (D5 609 / D6 98), Murphysboro 4 (D3 166 / D5 391) — which is the county stating a district line runs through them, and is why it sat measured-shut beside Bureau, Cumberland, Douglas, Ford, Knox and Piatt. **Its record named the answer and never followed it**: the county publishes its adopted Board District Map as a VECTOR PDF, and the seven districts are seven FILLED PATHS in the content stream — polygon objects whose exact fill colours pair one-for-one with the legend in legend order, each containing its own district numeral. That is the opposite of the colour-sampling the Fayette method rightly forbids: **read the fill OBJECTS, never the pixels.** Georeferenced to EPSG:3436 (which announced itself by matching the aspect ratio to four decimals where Web Mercator, UTM and Albers all missed), the fit lands at a 5.3 m mean boundary offset with the map's own scale bar confirming the scale. **NOTHING TRACED SHIPS**: the 53 unambiguous precincts take their district from the canvass and their geometry from the census voting districts, and the three split ones are resolved to whole census BLOCKS — the map only ever choosing between the two districts the canvass already named. Four verifications, none of them the map checking itself: 53/53 precincts land where the canvass says, 3/3 splits divide between exactly the two districts named, populations run 7,371-7,933 against a 7,568 ideal (worst 4.83%), and Murphysboro 4's population divides 30.2/69.8 where its registration divides 29.8/70.2 — a check **deliberately not claimed** for the two Carbondale precincts, which hold SIU dormitories and student apartments (one block is 700 people in FOUR housing units) where population predicts registration in neither direction. Two silent canvass traps are recorded with it: a row's last two values can share one line, which makes District 6 read as six precincts instead of seven, and the LAST board contest has no next-contest boundary, which makes District 7 read as 77. Both were caught by arithmetic — the distinct precinct count must equal the header's own "56 of 56" and the district registrations must sum to the countywide total — never by eye. The county's roster trap is the same shape: a member's job title is their DISTRICT for twelve of fourteen and their OFFICE for the other two, so the Chair's and Vice Chair's cards name no district and a title-keyed parser ships twelve seats for a fourteen-seat board; **the grouping heading is the authority**, cross-checked against each member's own directory entry. Jackson also FILLED ITS OWN NOTCH — all five Illinois neighbours already served, its western edge the Mississippi — so the ring count is unchanged at four and no hole opened or closed. **FIVE COUNTIES REMAIN SHUT FOR THE SPLIT-PRECINCT REASON and are now worth re-examining rather than re-probing** (Bureau, Cumberland, Douglas, Ford, Piatt): the question is no longer whether their districts are whole-precinct but whether they publish a VECTOR map, and a genuine raster scan is still shut because step one needs path objects (`scripts/build_jackson_boundaries.py`, `scripts/jackson_county_board_scraper.py`). **THAT SWEEP WAS RUN THE SAME DAY AND ITS YIELD WAS ONE COUNTY, REACHED BY A DIFFERENT ROUTE THAN THE ONE BEING TESTED.** DOUGLAS, THE 88TH (2026-08-23), NEEDED NO MAP READ AT ALL: douglascountyil.gov links an ArcGIS web map whose application config names ONE parcel service, while the ORG BEHIND IT (services3.arcgis.com/HXjsHxkFFcZquC8P) carries FIFTY-FOUR — `CountyBoardDistricts` and `VotingDistricts` among them, both shared public with empty licenceInfo. **A VIEWER SHOWS WHAT IT USES; THE ORG SHOWS WHAT THE COUNTY HAS**, which is the Richland finding for a second time and is now the FIRST thing to try, ahead of any map method, because it costs one request and returns geometry rather than an inference. So Douglas ships the county's own polygons AS DRAWN — no dissolve, no georeferencing, nothing derived — even though two of its seventeen precincts (Bourbon 2, Bowdre) are split between districts, which is exactly why nothing else could have drawn it. **THE CHECK IS A DIFFERENT COUNTY PRODUCT**: its certified 2022 General ran all seven districts and printed 2, 2, 3, 2, 3, 2, 5 precincts — nineteen across a county of seventeen, because the two split ones count twice — and overlaying the county's precinct layer on its district layer reproduces all seven exactly, both splits included; that agreement is a GATE in the builder. Its roster is assembled from two documents because the county publishes NO BOARD MEMBERS PAGE: the annual Yearbook names the seven people with a county e-mail each, and the certified summary reports give the district numbers. **THE YEARBOOK'S OWN NUMBERING IS LIST ORDER, NOT DISTRICT** — it lists "1. Tom Hettinger" where the returns put Hettinger in District 3 — so reading it as a district column misplaces six of seven, and the join is on surname with the builder failing if the two documents stop naming the same people. Six of seven e-mails ship: the yearbook prints District 1's on `douglascountuil.gov`, a typo for the domain the other six use, and a domain check drops it rather than shipping a dead address or silently correcting a character in someone's contact detail. The other four swept counties stay shut and now say why: FORD's maps are all raster (its Patton map is a hand-coloured scan) though its adopted 2021 reapportionment resolution was found and leaves the districts "To be determined" after the COVID census delay; BUREAU publishes no map and references no GIS host at all; CUMBERLAND's only surfaces are parcel and tax products; and PIATT's own GIS asserts "All Rights Reserved", which is the same answer the CCGISC licence gives (`scripts/build_douglas_boundaries.py`, `scripts/douglas_county_board_scraper.py`). VERMILION, THE 89TH (2026-08-23), IS THE BIGGEST COUNTY THE PROJECT HAS ADDED AND THE ONE WHOSE OWN GAP RECORD NAMED THE ANSWER AND WAS NEVER FOLLOWED. That record was long and almost entirely right — Clerk Wilson wrote "our county does not have shapefiles or GIS layers for precinct look ups", the nine district PDFs on the voter-maps page extract zero characters, and the county consolidated 84 census precincts to 38, closing the dissolve route — and it also contained the sentence that solved it: "a county GIS or assessor's desk, if Vermilion has one, is a different office and was not asked". Vermilion has one, publishing 26 public services to ArcGIS Online with empty `licenseInfo`, and it was found by an unauthenticated query to `arcgis.com/sharing/rest/search` with NO COUNTY PAGE READ AT ALL — which mattered, because the county's site could not be read when the sweep began. **THE CLERK'S "NO" WAS ACCURATE ABOUT THE CLERK AND WAS TAKEN AS AN ANSWER ABOUT THE COUNTY**, the Knox shape for the third time; that catalog query is now the FIRST check, ahead even of the org enumeration, because it costs one request and needs nothing from the county. Its counter-example shipped the same hour and is worth as much: the same sweep found Bureau's org — 63 services, parcels and soils and one per township, **not one board district or precinct** — so a record that said Bureau "references no GIS host at all" is now wrong about the host and MEASURED about the content. The county's site then turned out to be readable too, by the Coles pattern the record had already identified on 2026-08-21 and never acted on, and behind it sits a maintained 27-member table. **THE COUNTY PUBLISHES THREE BOARD-DISTRICT LAYERS AND THE WELL-LABELLED ONE IS OBSOLETE**: a service described as "Governmental and school districts in Vermilion County IL" holds the 2011 plan, identical to a `CountyBoardDistricts` layer whose roster columns stop at 2018, while the plan in force is an undocumented layer with a typo in its own name, `CountyBoardDistrcts2021`. **CURRENCY IS A MEASUREMENT, NOT A READING OF A NAME** — a plan drawn to a census balances on that census, and on the 4,943 Census 2020 blocks the shipped layer runs 8,209-8,298 (worst 0.7%) against the better-labelled one's 7,609-9,484 (15.1%) — and the builder GATES that comparison in BOTH directions, refusing to write if the superseded layer ever comes into balance too. Two traps are recorded with it. The members table publishes one of 27 names surname-first (`Auter, Tara`), which is flipped with the flip PRINTED every run, puts roles inside the name cell, hides a phone number in an e-mail cell, and seats two McFaddens in District 4 so surnames are not keys. And DISTRICT 9 NEARLY WENT MISSING: four of the Clerk's canvasses name districts 1-8 and never a 9th, and the plausible explanation already written into this project's own guidebook — that District 9 lies wholly inside Danville, which runs its own Election Commission — is WRONG, because District 9 is 80.6% inside the city by population while **District 8 is 95.99% inside and appears in the Clerk's canvass anyway**. The real cause is that the two authorities NAME CONTESTS DIFFERENTLY (`COUNTY BOARD 8TH DISTRICT MEMBER` against `CO. BD. MEMBER D8`), so searching one vocabulary returns ZERO matches in a commission report carrying three board contests; searched with both, all nine districts are certified. **NO PRECINCT LAYER SHIPS AND THAT IS A MEASUREMENT**: the org's precinct layer matches the Census 2020 fabric 84/84, which is exactly the problem, because the county's own canvasses have reported 38 clerk and 22 commission precincts since June 2022 — sixty current precincts drawn as eighty-four superseded ones (`scripts/build_vermilion_boundaries.py`, `scripts/vermilion_county_board_scraper.py`). **THE JOIN OPENED A THREE-COUNTY ENCLAVE AND THAT WAS PREDICTED WRONG BEFORE `--check` RAN, for the second county running**: the comment first written said Champaign and Piatt "keep their own outlet to the frontier through Ford", which quietly assumed Ford was an outlet rather than part of the block — Ford is unserved and its every other neighbour is served, so CHAMPAIGN, FORD and PIATT are one enclosed block, the fleet's second multi-county hole, the ring going 4 to 5 with the hole's bounds exactly the union of the three counties' extents and each carrying its own OUTSIDE anchor. KNOX (17095) was the third and is the one that shows enclaves are not permanent: enclosed by seven neighbours the day Warren shipped, it JOINED on 2026-08-22 as the 80th county and its hole closed, taking the mainland from four rings to three. An enclave is a county whose absence a reader can SEE, which is what makes it worth naming — and Knox is the proof that seeing it is how it gets fixed. So the mainland polygon carries FIVE rings — one outer plus four holes — and that is all `metro-outline.json` has; read the count from `build_metro_outline.py --check`, never from this sentence.** Menard was an enclave until it shipped on 2026-08-07, and the same join that merged the Effingham island created the Christian enclave on 2026-08-11 — the topology moves in both directions at once. Bureau and Christian are the single-county enclaves, and since 2026-08-23 the Clay/Fayette/Jasper/Marion/Wayne block and the Champaign/Ford/Piatt block are two larger ones — these are the absences a reader can see as doughnuts in the coverage wash. LAWRENCE is the counter-example that keeps the definition honest: Richland's join left it with no unserved Illinois neighbour either, and it is a NOTCH rather than a hole because it fronts the Indiana state line. Read the ring count from `build_metro_outline.py --check`, never from this sentence. The county-keyed honesty test is unchanged (never join for a rich statewide answer; a city never carries its unserved county). **The standing per-county view — service tier, board posture, dispatch entries, open gaps — is `docs/COUNTY_STATUS.md`, GENERATED by `scripts/build_county_status.py` from those lists + index.html's dispatch tables + `coverage-gaps.json`; regenerate after any county change (`--check` in CI fails otherwise, never hand-edit it).** `validate_index.py` fails the merge gate if a county gains a dispatch entry without joining the ring. Adding a county adds **dispatch entries and roster rows, never new layers** (`docs/EXPANSION_GUIDE.md` Part 2) — and some add roster rows ONLY: a county electing its board AT LARGE has no district geometry, so its members ride the County card via `data/app/il-county-commissioners.json` with no dispatch entry, no coverage function and no toggle. Nineteen counties ship that way (Monroe, Randolph, Pike, Brown, Calhoun, Putnam, Schuyler, Hamilton, Edwards, Greene, Morgan, Wabash, Moultrie, Massac, Saline, Gallatin, Union, Williamson, Alexander — Monroe, Randolph, Hamilton, Gallatin, Greene, Morgan, Moultrie and Calhoun carry dispatch entries besides, so the SERVICE-TIER count and this list are different questions: read the tiers from `build_county_status.py`, which counts a county on the card tier only when the card is ALL it has). SALINE, added 2026-08-21, is the largest at-large board in the fleet at thirteen members, and the first county whose form was settled with help from **ISBE's county-board structure table** — a 2007-vintage statewide table cited for STRUCTURE only, and only after it was checked against four counties whose current pages this project can read (Clay, Hancock, Lawrence, Adams) and matched all four. The decisive source is still the county's own certified canvass: "FOR MEMBERS OF THE COUNTY BOARD / Precincts Counted 28 of 28 / (Vote for not more than seven)" over all 15,441 registered voters. MASSAC, added 2026-08-21, is the fleet's FOURTH ISLAND and the second county ever to join through the at-large tier alone (after Edwards): its board form was proven from the Clerk's own results report — "FOR COUNTY COMMISSIONER / Precincts Counted 17 of 17 / (Vote for one)" over all 11,265 registered voters, with the countywide Clerk contest on the same page as the control — and its three commissioners come from the county's own page, never from the returns. It was reached by probing the domain in `data/app/il-county-clerks.json` rather than permuting the county's name, the Cumberland correction applied to the whole frontier at once; four other records had claimed "no county website" about counties whose clerk domain answers 200. HANCOCK, the 75th (2026-08-21), is the first county built from a COUNTY-RUN RESULTS DATABASE: its Clerk publishes every contest back to 2020 at electionstats.hancockcounty-il.gov — the county's own, not a vendor's — and each contest page carries a PER-PRECINCT table, so a board-district contest NAMES the precincts that voted in it rather than only counting them. That is precisely what Knox's and Richland's canvasses withhold, and it is the whole difference between a county that builds and one that does not; when a county site offers "previous election results", look for a per-precinct table before writing to anybody. Two witnesses per district (2022 and 2026 agree exactly, 2024 re-confirms four of five), an exact 33-precinct partition and a 33/33 Jasper pass, with one census TYPO aliased and named — MONTIBELLO IV for Montebello 4 (`scripts/build_hancock_boundaries.py`). GALLATIN, the 76th (2026-08-21), is the county the fleet had already written off, and the reason it was written off is the finding: on 2026-08-20 its record said the county was "DARK to this client on every route tried" and that its Clerk's own mail domain "does not resolve at all". Neither was true. gallatinco.illinois.gov answers 200 with 247 KB and serves the **incomplete TLS chain** Coles taught this project to recognise — the leaf without its Sectigo intermediate — so every automated client reported a failure indistinguishable from an absent host while the site loaded fine in a browser. THAT MAKES IT A PATTERN RATHER THAN ONE COUNTY'S MISCONFIGURATION, and it is now swept for: probing the 27 frontier counties' clerk domains the same afternoon found Gallatin and confirmed VERMILION, whose record had likewise said its site "is not reachable from this project's network" (`scripts/probe_incomplete_tls_chains.py`, which reports the intermediate's SHA-256 so a scraper can PIN it; the chase itself lives in `il_county_commissioners_scraper.py` and `coles_county_board_scraper.py`, and never by disabling verification). Gallatin joins on BOTH tiers at once: its five board members ride the County card, proven countywide twice from its own canvasses ("CO.BD.MEMBER CWD (VOTE FOR) 3" in 2026, "COUNTY BOARD MEMBER CWD (VOTE FOR) 2" in 2024), while its eleven precincts dispatch — the Census 2020 voting districts ONE FOR ONE, a Jasper pass of 11/11 with the population identity to the person, corroborated by the county's own legible precinct map and by canvasses that count eleven and name nine (`scripts/build_gallatin_precincts.py`). KNOX, the 80th (2026-08-22), is the first county to join by CLOSING AN ENCLAVE rather than extending the frontier, and the lesson is in how it was blocked: its record said knoxcountyil.gov "refuses every request", which is TRUE OF THE WEBSITE and was written as though it described the county. Knox has FOUR hosts. The site denies; gis.knoxcountyil.gov is a public ArcGIS Server serving the COLES PATTERN and carrying the county's 20 townships; cms2.revize.com serves the same website's DOCUMENTS with no block at all; and knoxclerk.org, cited by the county's own 2022 board packet, is simply dead. A COUNTY IS NOT BLOCKED BECAUSE ITS WEBSITE IS — and `scripts/probe_incomplete_tls_chains.py` now sweeps `gis.<domain>` as well as the bare and www clerk domains, because the old host list could never have found the one host that mattered. The districts come from the county's own adopted map ("Knox County Board Districts 2022", 01/28/2022) on which the DASHED line is the district boundary and the thick ORANGE line the precinct boundary; districts 1-3 are taken as the CITY OF GALESBURG publishes them, since it runs its own Board of Election Commissioners, and 4 and 5 are unions of whole townships minus that city. THREE WITNESSES agree — the map, the certified returns' 13-and-15 precinct counts, and Census populations within 6.3% of ideal — and a fourth method, sampling the map's FILL COLOURS, was tried first, produced a clean and confident answer, and was WRONG, because those fills are per-township decoration; the precinct-count arithmetic caught it, which is why that arithmetic is a gate in the builder rather than a comment. Its roster is the ROLL CALL of the county's own minutes, absent members read exactly like present ones, every row naming the meeting (`scripts/build_knox_board_districts.py`, `scripts/knox_county_board_scraper.py`). UNION and WILLIAMSON, the 81st and 82nd (2026-08-22), are the first counties whose ROSTERS come from certified returns with no page read at all: both sit behind an sgcaptcha gate answering HTTP 202 with a 169-byte meta-refresh, the "202 is never a document" case, and NOTHING HERE TRIES TO DEFEAT A CHALLENGE — a captcha is an access control, not an obstacle to route around. Their election authority publishes elsewhere, so each commissioner is whoever the county certified as elected in the most recent general that seated their seat, every row naming that election (the Clark posture, applied to an at-large board). THE COUNTYWIDE TEST IS A GATE, and it caught a stale STATE record: ISBE's 2007 county-board structure table calls Union "Single-Member" with five districts, while Union's own returns count all five lettered seats in 20 of 20 precincts, Seat A polling 6,694 votes against the countywide State's Attorney's 6,738 in the same canvass — a fifth of a 17,244-person county would poll about 1,300. The returns are current and the table is not, so the builder refuses to write unless every commissioner contest was counted in every precinct the canvass reports (`scripts/platinum_canvass.py`, `RETURNS_ROSTERS` in `scripts/il_county_commissioners_scraper.py`). ALEXANDER, the 83rd (2026-08-23), is the first county to join with its roster KNOWINGLY SHORT, and the reason is a card fix rather than a new source: its board seats three, its own page publishes two, and the third seat is named by nobody. The record had refused to ship it on the grounds that "shipping a three-seat board with two names would conceal a seat rather than invent one" — true of a card that says nothing, and the districted counties had been saying it since Livingston ("1 of 3 seats not listed in the county's directory"). The at-large County card simply had no way to, because no at-large county had ever published fewer members than it seats; now the roster block carries `seats` beside its members and the shortfall renders in the same words, so a card that names two and states a third is unlisted conceals nothing. Its form is proven from the county's own 7 May 2024 minutes (Chairman / Vice Chairman / Commissioner in the letterhead) and from a certified canvass a THIRD test of the vendor list turned up — platinumelectionresults.com does carry Alexander at county id 46, which an earlier record had denied after testing only the pollresults/accessliberty pair, and its 2024 General reports "FOR COMMISSIONER" in all ELEVEN precincts, the same denominator that ballot's presidential contest reports. **Its trap is a NAV MENU that outlived its subjects**: every page carries a hand-built "County Board Chairman / Member / Vice Chairman" trio pointing at /team/ slugs, two of which are 404s of people out of office — so a scrape of /team/ links would ship four commissioners for a three-seat board, two of them wrong, where the Elementor team widget on the board page is the county's maintained list. CUMBERLAND, the 77th (2026-08-21), is the first county to join **on its precincts while its board stays permanently shut**, and it is the case that sharpens the join bar settled the same day (the board OR the precincts). The three counties that were precinct-only before it — Calhoun, Morgan, Gallatin — elect their boards AT LARGE, so there is no district for a precinct to belong to; Cumberland IS districted, six members from three compass-point districts, and those districts CUT THROUGH precincts, which its own certified 2026 General Primary states by counting them: Central 6 + Eastern 5 + Western 3 = fourteen against a county of twelve, identically for both parties, while every countywide contest on the same canvass reports exactly twelve. So nothing rides the County card for it either — a districted county's members on the at-large card would read as elected countywide — and the precinct answer is the whole of what the county gains. Its build also corrected a prediction this project had written into the record: an alias was expected, because the Clerk's polling notice groups precincts as "NEOGA I & II" and "GREENUP I, II, & III" where the census writes arabic. **Those are polling-place groupings, not precinct names** — three buildings serving seven precincts — and the county's own returns print GREENUP 1/2/3 and NEOGA 1/2 exactly as the census does, so the Jasper test passes 12/12 with no alias at all. READ PRECINCT NAMES FROM A CANVASS'S CONTEST LIST, NEVER FROM A POLLING NOTICE'S HEADINGS. Its publisher is `results.gbsvote.com`, **thirteen** Illinois counties, of which two are still unserved (Jasper, Knox). That platform was NOT a new find and an earlier version of this paragraph wrongly said so: `docs/DATA_LAYER_GUIDEBOOK.md`'s backlog recorded and swept it on 2026-08-20 — the failure was that the sweep's results never left the backlog, so Johnson's and Perry's records went on calling their board form undeterminable for another day. **A measurement filed in a backlog and nowhere else is a measurement the next pass repeats**, which is why the platforms now sit in one table beside the method sections. What WAS new that day is Clarity/Scytl (`results.enr.clarityelections.com/IL/<County>/`), twelve Illinois counties, eleven already in the ring (`scripts/build_cumberland_precincts.py`). JOHNSON AND PERRY, the 78th and 79th (2026-08-21), are the first counties in the fleet reached **without their own websites being read at all**, and that is the generalisable part: a blocked county SITE is not a blocked county, because a county's ELECTION AUTHORITY is a separate publisher and often a separate host. Both counties' blockers are unchanged — Johnson's site sits behind a host-level block, Perry's refuses this client — and both joined anyway, because their certified canvasses publish on `results.gbsvote.com`, where this project had already measured both board forms on 2026-08-20 and left the answer sitting in a backlog. Each county's 2026 General Primary carries a single countywide FOR COUNTY COMMISSIONER contest per party (16 of 16 in Johnson, 27 of 27 in Perry) and no district-suffixed board contest anywhere, settling both boards AT LARGE from a certified document, and prints a committeeperson contest per precinct that NAMES every precinct — Jasper 16/16 and 27/27 with the population identity to the person. **Neither roster ships**: commissioners are a County-card fact, a certified return names who WON rather than who holds the seat today (the Scott reasoning), and the only county source for each is the site nothing here can read — so these two join on precincts ALONE where Gallatin joined on both tiers. What the vendor gives is worth stating exactly, because it bounds the route: SUMMARY canvasses with a per-contest precinct COUNT, which NAME a county's precincts and SIZE its districts and never compose them. **Johnson's join also ENDED THE MASSAC ISLAND** hours after it formed, dropping the outline from five rings across two polygons to four rings in one (`scripts/build_johnson_precincts.py`, `scripts/build_perry_precincts.py`). FOUR COUNTIES GAINED PRECINCTS ON 2026-08-21 WITHOUT ANYTHING HAVING BLOCKED THEM — CASS, GREENE, SCOTT and MOULTRIE — and the reason they were missing is the point: **all four were already served, and none had a gap record saying its precincts were absent.** No refusal, no missing publisher, no split fabric; simply nothing on file to notice, which is how a county goes unbuilt for weeks while every guard in the repo stays green. They came out of an audit of served counties WITHOUT a precinct layer AND WITHOUT a record explaining it — the shape of audit worth repeating, because a gap record is the only thing that makes an absence visible. Each is the census fabric one for one, named by the county's own certified 2026 General Primary: Cass 21/21 with no alias, Greene 22/22, Scott 10/10, Moultrie 16/16, populations exact. Two of them settle how a name difference is decided, in opposite directions: Greene's `WRIGHTS 2` against the census's `WRIGHTS` is a NUMBER THE COUNTY ATTACHES, so the county wins and the alias renames the census; Scott's `MERRIT` against `MERRITT` is a DROPPED LETTER in the results feed and Merritt is the township's name, so the census wins and the shipped label comes from the spelling the alias displaced. The build also fixed a latent defect in the shared `title_case`, which had been lowercasing the second half of hyphenated names and turning roman numerals into words — `Crater-carlin` and `Rocky Run-wilcox` were already shipped that way inside Calhoun's and Hancock's files (`scripts/vtd_board_districts.py`). MENARD AND SCHUYLER closed the set the same evening, and neither came from a results platform. Menard's precinct map is a **vector PDF with a text layer** — rare in a fleet where such maps are almost always scans — so look for one before reaching for the raster methods. Schuyler cost a wrong turn worth recording: its Clerk's mail domain `schuylercountyil.gov` **has no A record at all**, so the clerk-domain rule does not reach it, while its real site `www.schuylercounty.org` had been in this repo's own scraper table since 2026-08-02 — WHEN A CLERK DOMAIN DOES NOT RESOLVE, CHECK THE SCRAPERS. Its name list is a polling table that GROUPS EXPLICITLY ("Buena Vista #1 & #2"), which is exactly the ampersand Cumberland's notice lacked, and it turned up the **second county-run results database** after Hancock's — official, but CUMULATIVE, so it confirms a count and names only three of seventeen. A county-run database is not automatically a per-precinct one. GREENE, added 2026-08-08, is the first that was ALREADY SERVED before its board arrived: a 7th-Circuit subcircuit already answered there, so it moved from the judicial tier to the County card without changing the ring or any anchor. EDWARDS AND WABASH ARE THE TWO WHOSE ROSTERS ARE NOT SCRAPED: neither has a working website (Edwards has none at all; Wabash's domain carries mail and serves no page), so their commissioners ride `DOCUMENT_ROSTERS` in `scripts/il_county_commissioners_scraper.py` — Edwards's carried from a document its Clerk sent, Wabash's from its Clerk's e-mail of 2026-08-16 (names only: her e-mail lists home addresses, which never ship, and no chairman is marked until she answers which of the three chairs) — and the weekly run prints a NOT RE-READ line per county naming the document and its age rather than pretending either was re-read.

**There is no build step, no framework, and no server-side code.** The entire app — styles, core, and all layer modules — lives inline in `il/index.html` (~26,000 lines). `il/sw.js` is the service worker; `il/data/app/*.json` are runtime-fetched data files. Everything else is data pipeline, scrapers, or CI.

**THE APP LIVES UNDER `il/` AND SERVES AT `/il/`** (R2.3 — `docs/DEV_PROCESS_ASSESSMENT.md`). The repo is consolidating into one repo, one site: each state instance is a folder that serves at its own path, so the root can become the fleet landing page. The repo ROOT holds the **fleet landing page** (`index.html`) — Districtry's front door, listing the places the fleet answers for — plus a service-worker KILL SWITCH (`sw.js`) that retires the old root registration without touching the instance's own. Repo-level files stay at the root — `metro-worksheet.json`, `metros.json`, `sitemap.xml`, `robots.txt`, `CNAME`, the IndexNow key, `fonts/` (the landing page's own self-hosted Barlow, distinct from `il/fonts/`), `scripts/`, `docs/`. **Never re-add an app file to the root**, and note there is deliberately NO `data -> il/data` symlink: 60 roster workflows gate their PR step on `git diff --quiet -- il/data/app/`, and git does not traverse a symlink, so a shim there would make every refresh run green while silently never opening a PR again.

**The root landing page is GENERATED** (`scripts/build_landing_page.py`, `--check` in CI), and that is the point rather than a convenience. The assessment's central finding was that brand identity had decayed into scattered literals — 98 files carrying `chidistricts`, seven strings locked inside engine fences, a redesign built twice because `index.html` could not be parameterised. A hand-written front door whose state list is HTML would reproduce that on day one of the fix, so every fact on the page comes from a file that already owns it: `metros.json` (the fleet list — each entry's `tag`, `landing_name`, `blurb`, `url`; `sync_fleet` projects those three landing fields away, so they never reach an app), `districtry/tokens/districtry.tokens.css` (light + dark tokens, extracted by name so a rename fails the build), `districtry/icons/favicon.svg` (inlined as a data URI) and `fonts/barlow-fontface.css` (`build_fonts.py landing`). Adding a state is a manifest entry and a regenerate.

**The root still FORWARDS an old app link, and that is load-bearing.** Before R2.3 the Illinois app served from this origin's root, so every share link and embed snippet it handed out was built from the root URL (`/?utm_source=share&utm_medium=link#point=…`, and the `iframe` equivalent) — those sit in other people's pages and bookmarks and cannot be recalled. A landing page that simply replaced the redirect stub would answer all of them with a page about Illinois instead of the map they asked for. So a URL carrying app parameters (a `#point=`/`#layers=`/`#zoom=` permalink, or the share/embed campaign tags) still forwards to `/il/` with query AND hash intact, and the landing page renders only for a bare visit; the guard runs in `<head>` before the body paints, so a forwarded visit never flashes it. `scripts/landing_test.mjs` asserts that in a real browser both ways and runs in CI beside the three instance smoke tests — a regex typo there would break other people's embeds silently, on a page that still looks right and still passes its drift check.

**`sources.html` is the app's one sub-page**, and it is where provenance lives. The footer used to carry a ten-link credit row, which could name the publishers but could never answer the question a reader actually has — *where did THIS answer come from?* The page keeps those credits and adds the **layer matrix**: one row per registered layer naming what it answers, the publisher its boundary comes from (linked), where the names on its card come from, and the ground it answers on. It is **GENERATED** from `metro-worksheet.json`'s `layers[]` — the same list that drives `EXPECT_LAYER_IDS`, `LAYER_AREA_RANK` and the smoke test's layer count — so a layer cannot ship without a row and a row cannot outlive its layer; `layers[].source` is now required for every layer (the generator refuses otherwise). The reciprocal move sent the **Data gaps panel's button to the masthead**: absence and provenance are the same reader's question, and the caveat was invisible at the foot of a long page. Both keys (`sources_page`, `verified_date`) are OPT-IN — a sibling fork that carries neither generates nothing and sees a byte-identical file, which is the rule for anything riding the engine release channel.

<!-- ==== GENERATED:BEGIN metro-facts ==== -->
**Metro facts** (generated from `metro-worksheet.json` — edit the worksheet and run
`python3 scripts/generate_metro_files.py`; hand-edits here fail CI):

- Metro: Chicago (`chicago`) — https://districtry.com/il/
- Geocoders: address Photon (Chicago-bounded type-ahead); unbounded Photon (whole-coverage, sibling-metro lookup); POI Nominatim (office-address pin lookup, Illinois-bounded, serial >=1s queue)
- Ground truth: 41.88250,-87.62850 (downtown Loop — inside Cook County) → school-board 12; il-supreme-court 1; ccbr 3. Negative point 41.70000,-87.10000 (Lake Michigan, Indiana waters — outside all three anchor layers).
- Layers: 39 registered (political 11, safety 7, schools 9, geography 12); `registerLayer(` floor 17. Debug namespace `window.ChiExplorer`.
- Scheduled workflows: `update-ilga-roster.yml` (Mon 13:00 UTC); `update-congress-roster.yml` (Mon 13:00 UTC); `update-cpd-roster.yml` (Tue 13:00 UTC); `update-ccpsa-roster.yml` (Wed 13:00 UTC); `update-will-county-board-roster.yml` (Thu 13:00 UTC); `update-kendall-county-board-roster.yml` (Thu 14:00 UTC); `update-mchenry-county-board-roster.yml` (Thu 15:00 UTC); `update-kane-county-board-roster.yml` (Thu 16:00 UTC); `update-lake-county-board-roles.yml` (Thu 17:00 UTC); `update-ccbr-roster.yml` (Fri 13:00 UTC); `update-county-clerk-roster.yml` (Sat 13:00 UTC); `update-dupage-county-board-roster.yml` (Sun 13:00 UTC); `update-winnebago-county-board-roster.yml` (Wed 15:00 UTC); `update-sangamon-county-board-roster.yml` (Tue 15:00 UTC); `update-livingston-county-board-roster.yml` (Tue 14:00 UTC); `update-municipal-officials.yml` (Wed 14:00 UTC); `update-dekalb-county-board-roster.yml` (Fri 14:00 UTC); `update-ogle-county-board-roster.yml` (Sat 14:00 UTC); `update-stephenson-county-board-roster.yml` (Sun 14:00 UTC); `update-carroll-county-board-roster.yml` (Mon 14:00 UTC); `update-lee-county-board-roster.yml` (Tue 16:00 UTC); `update-rock-island-county-board-roster.yml` (Wed 16:00 UTC); `validate-sources.yml` (1st of month 14:00 UTC); `update-lasalle-county-board-roster.yml` (Sun 15:00 UTC); `update-henry-precinct-polling.yml` (Fri 18:00 UTC); `update-woodford-county-board-roster.yml` (Mon 16:00 UTC); `update-logan-county-board-roster.yml` (Mon 17:00 UTC); `update-boone-county-board-roster.yml` (Tue 17:00 UTC); `update-grundy-county-board-roster.yml` (Wed 17:00 UTC); `update-jefferson-county-board-roster.yml` (Thu 19:00 UTC); `update-macon-county-board-roster.yml` (Fri 17:00 UTC); `update-menard-commissioners-roster.yml` (Thu 20:00 UTC); `update-montgomery-county-board-roster.yml` (Wed 18:00 UTC); `update-henry-county-board-roster.yml` (Thu 18:00 UTC); `update-peoria-county-board-roster.yml` (Fri 15:00 UTC); `update-tazewell-county-board-roster.yml` (Fri 16:00 UTC); `update-iroquois-county-board-roster.yml` (Sat 15:00 UTC); `update-county-commissioners-roster.yml` (Sat 16:00 UTC); `update-dewitt-county-board-roster.yml` (Sun 15:00 UTC); `update-washington-county-board-roster.yml` (Sun 16:00 UTC); `update-cass-county-board-roster.yml` (Sun 17:00 UTC); `update-marshall-county-board-roster.yml` (Mon 18:00 UTC); `update-mcdonough-county-board-roster.yml` (Mon 19:00 UTC); `update-fulton-county-board-roster.yml` (Tue 20:00 UTC); `update-stark-county-board-roster.yml` (Tue 19:00 UTC); `watch-mason-roster-source.yml` (Tue 18:00 UTC); `update-shelby-county-board-roster.yml` (Wed 19:00 UTC); `update-white-county-board-roster.yml` (Sat 17:00 UTC); `update-jodaviess-county-board-roster.yml` (Sat 18:00 UTC); `update-coles-county-board-roster.yml` (Sun 18:00 UTC); `update-clark-county-board-roster.yml` (Mon 20:00 UTC); `update-crawford-county-board-roster.yml` (Tue 21:00 UTC); `update-mercer-county-board-roster.yml` (Wed 20:00 UTC); `update-edgar-county-board-roster.yml` (Thu 21:00 UTC); `update-franklin-county-board-roster.yml` (Fri 19:00 UTC); `update-clinton-county-board-roster.yml` (Sat 19:00 UTC); `update-adams-county-board-roster.yml` (Sat 20:00 UTC); `update-knox-county-board-roster.yml` (Sun 20:00 UTC); `update-warren-county-board-roster.yml` (Sun 19:00 UTC); `roster-health.yml` (daily 23:00 UTC); `update-hancock-county-board-roster.yml` (Fri 20:00 UTC); `update-richland-county-board-roster.yml` (Mon 21:00 UTC); `update-jackson-county-board-roster.yml` (Tue 22:00 UTC); `update-douglas-county-board-roster.yml` (Wed 21:00 UTC); `update-vermilion-county-board-roster.yml` (Thu 22:00 UTC).
- Source registry: `scripts/validate_sources.py` (machine-checked monthly)
<!-- ==== GENERATED:END metro-facts ==== -->

## Running & testing

```bash
# Run locally — any static server works; internet needed for live-API layers:
python3 -m http.server 8000    # then open http://localhost:8000/

# Behaviour gate (real Chromium boot via Playwright) — the main test:
npm install playwright@1.56.1 && npx playwright install --with-deps chromium
BASE_URL=http://localhost:8000/il/ node scripts/smoke_test.mjs   # serve first, then run

# Static gate (run after any data/app regeneration or app edit):
python3 scripts/validate_index.py il/index.html

# Generated-region gate (Conversion 2): per-fork facts live ONCE in
# metro-worksheet.json; GENERATED:BEGIN/END regions in index.html, sw.js,
# sources.html, validate_index.py, smoke_test.mjs, CLAUDE.md, and README.md are
# emitted from it. NEVER hand-edit a GENERATED region — edit the worksheet and
# regenerate:
pip install -c scripts/requirements.txt jsonschema
python3 scripts/generate_metro_files.py            # regenerate in place
python3 scripts/generate_metro_files.py --check    # the CI drift gate

# Source-freshness gate (checks upstream datasets haven't gone stale):
pip install -c scripts/requirements.txt requests
python3 scripts/validate_sources.py            # add --offline to skip network

# Link gate (checks every URL the cards and rosters actually render):
python3 scripts/validate_card_links.py         # --offline lists the surface only

# Roster retention gate (fails when a field stops being published):
python3 scripts/check_roster_retention.py --base origin/main

# (The state-template gate and its BUILDER were both RETIRED — docs/EXPANSION_GUIDE.md
# §4.10 unhooked the gate, and R2.1 deleted the builder along with the rest of
# the per-state fork machinery: docs/DEV_PROCESS_ASSESSMENT.md. The template
# repo is archived and a state now expands IN PLACE here as its own folder.
# The TEMPLATE:BEGIN/END markers in il/index.html and il/sw.js survive on
# purpose — they are the line-by-line metro-vs-engine map, which is exactly the
# fragment-boundary data the composer reclaims at R3. Do not sweep them.)
```

`smoke_test.mjs` is a single end-to-end script, not a framework — there are no "individual tests" to select. It asserts the app boots, registers all layers, classifies a known downtown point against ground truth (school board 12, IL Supreme Court 1, Board of Review 3), and degrades to an isolated error card when a source fails. `node_modules`/`package.json` are intentionally gitignored — this repo never commits build artifacts.

**Sandboxed environments (Claude Code web) — Leaflet CDN egress:** every instance's `index.html` loads Leaflet from `cdnjs.cloudflare.com`. In the Claude Code web/sandbox the headless browser cannot reach that CDN — Chromium doesn't use the agent HTTPS proxy, so the request resets (`ERR_CONNECTION_RESET` → `L is not defined` → the app never boots). This is environmental, **not** a code regression; don't chase it in app code. It's handled automatically: a `SessionStart` hook (`.claude/settings.json`) runs `scripts/vendor_leaflet.sh`, which `curl`s Leaflet (curl *does* go through the proxy) into **each instance's own** vendor dir — `scripts/vendor/leaflet/` for `il`, `sf/scripts/vendor/leaflet/`, `nyc/scripts/vendor/leaflet/` — because that is where each instance's `smoke_test.mjs` looks (it resolves `vendor/leaflet` next to itself). All three are gitignored; the smoke test then serves them same-origin via `page.route`, so the app boots. Production and GitHub Actions CI are untouched — they reach the CDN directly and the vendor dirs are absent, so the fallback is skipped. To vendor one instance manually, `bash scripts/vendor_leaflet.sh sf`; with no argument it does all three.

**That script is ONE script, and it fails loudly on a misconfiguration.** Three byte-identical copies used to live in `scripts/`, `sf/scripts/` and `nyc/scripts/` — genuinely non-redundant while the instances were separate repos, because each self-located into its own tree, and pure duplication the moment they became folders under one root where only the root copy's hook ever fires. It also used to print a soft note and `exit 0` when it found no Leaflet URL, which is right for an unreachable CDN and wrong for a bad path: R2.3 moved the app to `il/` and left the script reading the repo-root `index.html`, now the redirect stub, so it skipped silently and the smoke test died 45 seconds later at `page.waitForFunction: Timeout` — the exact symptom the paragraph above tells you not to chase into app code. A missing app file or one with no Leaflet URL now exits 1 and names the instance; only a failed fetch stays best-effort.

`validate_index.py` is the merge gate: it confirms `index.html` passes `node --check`, still registers every layer (a drop in the `registerLayer(` count fails), embeds no dataset inline, that every `data/app/` file is present with the expected feature/roster counts, and that **`sources.html` still carries a matrix row for every registered layer and the app still links to it** — a layer whose provenance row is missing reads to a reader as a layer with no source, and a sources page nothing points at is a page nobody reads.

`validate_sources.py` is the **freshness** gate (complements the merge gate above). It catches the failure mode the roster scrapers can't: a publisher silently superseding a dataset the app hardcodes. Chicago Data Portal (Socrata) datasets are re-published under a **new id** each year — the CPS attendance-boundary layers ship a fresh `…SY####` dataset every school year — so the old id keeps returning stale data with no error. The script carries a manifest of every Socrata dataset id and shapefile provenance URL the app depends on and checks: (1) the manifest still matches index.html (drift guard), (2) each Socrata id still resolves and keeps its expected name, searching the portal catalog for a **newer-year edition** of the year-versioned ones, (3) the three shapefile sources (ilsenateredistricting.com / illinoiscourts.gov / cookcountyboardofreview.com) are reachable and their built `data/app/` files present, (4) the live ArcGIS/TIGERweb endpoints resolve. It **never edits the app** — swapping a dataset id is schema-sensitive — it exits non-zero only on hard FAILs and reports a newer edition as a WARN. `.github/workflows/validate-sources.yml` runs it monthly and opens/updates a single tracking issue on any WARN/FAIL (the job stays green; the issue is the signal), matching the roster pattern of "source changes get a human look before they ship." When a dataset id is swapped in index.html, update the manifest in `validate_sources.py` to match. **A source whose block is permanent and measured carries `"blocked": "<reason>"`** — seven do (Joliet, both McHenry sources, both Kendall sources, Lake board roles, the Chicago BOE early-voting page). For those the check INVERTS: unreachable is reported OK ("unreachable AS EXPECTED", still visible in the report), and becoming REACHABLE again is the WARN, because that is the state a human can act on — it means automation may be able to resume. Before that flag existed the monthly issue reopened with the same seven no-op WARNs every month. **HTTP 202 counts as unreachable**, not reachable: "Accepted" is never a document, and it is what the captcha fronts in front of several county sites return (dekalbcounty.org moved behind one around 2026-07-31 — its board scraper failed outright while this validator went on calling the source reachable, because 202 < 400).

`validate_card_links.py` is the **link** gate, and it watches the surface neither of the other two does: the URL a reader clicks. `validate_sources.py` covers dataset endpoints; nothing covered the `primaryLink` on a card or a roster's `sourceUrl` until Macon's card shipped `maconcountyil.gov` — a hostname with **no DNS record at all** — for three days (the county is at `maconcounty.illinois.gov`). It EXTRACTS its surface rather than carrying a manifest: `url:`/`href=` literals in **every authored HTML page** (index.html and sources.html) plus every http string in `data/app/*.json`, ~1,200 URLs across ~540 hosts, so a new county is covered the day it ships with nothing to update. sources.html joined that list the day it shipped, and had to: it took ~40 authored URLs off index.html — the footer credit row plus one boundary link per layer — which scanning index.html alone would have silently dropped off this gate. Three things make its output readable instead of a monthly wall of other people's outages. (1) **Authored vs published.** A URL this repo chose (index.html, a builder's own `SOURCE_URL`) FAILs when dead — it is ours to fix. A URL carried through a roster (the `url`/`profileUrl` fields: a village's website exactly as its county clerk published it, one of which is literally `yahoo.com`) is capped at WARN, and a mere 403 on one is OK — CDN-fronted village sites refuse datacenter clients as a matter of course and there is nothing to do about it. (2) **`EXPECTED_UNREACHABLE`**, the same inversion as `validate_sources.py`'s `blocked` flag and for the same measured reason: six hosts are recorded as refusing this client (Cloudflare challenges, Akamai denials), refusal reads OK, and REACHABLE-again is the WARN. An entry cannot calcify — it also warns if the app stops citing the host, and the inversion caught its own author within an hour: a host recorded off a 429 answered 200 on the next run, which is why 429 is now treated as rate-limiting (back off, retry, and say the probe may have caused it) rather than as a block. (3) **Runtime-built URLs are not probed.** `url: "…/schoolprofiles/" + schoolId` is a prefix, not an address; cps.edu 404s it bare while every built per-school URL resolves, so concatenated literals are named in the report instead of reported dead. Two lessons are baked in from getting them wrong first: DNS is resolved **once per host with retries** (a flaky parallel lookup reported two live sites as having no DNS record), and a 403 from an egress proxy is not a 403 from a site — in the Claude Code sandbox github.com 403s with the proxy's own JSON body, so never record a host from a local run without checking who answered.

`check_roster_retention.py` is the **retention** gate, and it exists because every other roster guard measures the wrong thing. Each builder floors a COUNT — "Boone seats 12", "Brown seats 7" — and on 2026-08-08 every one of those passed while Brown County's seven published e-mail addresses were one commit from deletion: browncoil.org had switched on Cloudflare's e-mail obfuscation, so `mailto:` became `data-cfemail`, and the parser returned seven members carrying nothing. **Seven rows in, seven rows out, guard satisfied, contact gone.** Thirty of the 43 builders additionally floor a field or two (`MIN_EMAILS`, `MIN_PHONES`), which is better, but each floor is hand-set per county, 13 builders have none, and no floor anywhere covers a field nobody thought to name — a new field ships unguarded by construction. So this gate lives outside the builders: it compares every `data/app` roster against the same file at the change's base and asks whether each field still appears on about as many records as before. The shipped file is the baseline, so a field is protected the moment it first ships, with nothing to configure. It fails on a field that **vanishes** (was on ≥2 records, now none), on one that loses at least half its records AND at least 3 of them, and on a record count halving. It ignores new files, new fields and small drops — a member leaving moves these by ones and twos. **The subtlety that cost the first draft its first test: coverage is measured PER SOURCE, not per file.** Pooled across `il-county-commissioners.json`, Brown's seven vanishing e-mails read as 40 → 33, an 18% dip that passed every threshold; ten counties share that file and each can break alone, so each top-level key is measured on its own (files pooling more than 200 sources, like `municipal-officials.json`, stay file-level, where per-group thresholds would fire on every village that reshuffles a page). A legitimate mass change — a consolidated election emptying a column — is recorded in `ACCEPTED_DROPS` with a reason and a date rather than by loosening a threshold for everyone. Wired into `smoke-test.yml`, which is where roster refreshes land as bot PRs.

## Architecture: stable core + pluggable layer modules

All inside `index.html`, wrapped in one IIFE. The full contract and per-thread build log live in `docs/BUILD_PLAYBOOK_1.md`; `docs/OPTIMIZATION_PLAYBOOK.md` holds measured optimization tasks. `docs/DATA_LAYER_GUIDEBOOK.md` is the fleet-wide layer inventory (concept × metro matrix, recorded drops, parity debts, backlog) — update it in the same change as any layer add/rename/remove; the weekly fleet-status run WARNs on drift. `docs/EXPANSION_GUIDE.md` is the **primary deployment guide for all future expansion** — Part 1 the governance taxonomy and county-expansion invariant (a new county adds dispatch entries and roster rows, never new layers), Part 2 the new-Illinois-county recipe, Part 3 statewide growth, Part 4 the new-metro-fork port (Chicago is the reference implementation; each metro is its own fork — completed port records live in `docs/archive/`), Part 5 the new-concept procedure — consult it before any expansion or new-concept proposal. It supersedes the retired `METRO_EXPANSION` / `STATEWIDE_EXPANSION` / `COUNTY_LAYER_CONSOLIDATION` / `MUNICIPAL_COUNCILS` / `ILLINOIS_LAYER_STANDARDIZATION` docs (pointer stubs remain at the old paths; originals verbatim in `docs/archive/`).

**Core** provides: the Leaflet map, click-to-select + debounced Chicago-bounded Nominatim geocoder, a global `state` object `{selectedPoint, sequence, layersOn, ...}`, the layer registry + result-card framework, selected-boundary highlight, and URL-hash permalinks (`#point=lat,lng&layers=ward,school-board`). A small namespace is exposed as `window.ChiExplorer` for debugging.

**Shared utilities** (reuse these; don't reinvent):
- `sanitize(str)` / render via `textContent` — all external strings must go through one of these. Injecting scraped or API text as HTML is treated as a real security bug here.
- `pointInGeometry(pt, geometry)` — the point-in-polygon test every polygon layer's `query` uses.
- `fetchJSONWithRetry(url, opts, retries)` — the standard data-fetch path (retry + failure isolation).
- `haversineMiles(...)` — for the nearest-N layers, which use straight-line proximity instead of point-in-polygon (police/fire stations via the `registerNearestPointLayer` factory; `school-site` as a bespoke block).

**A layer module** is registered via `registerLayer({ id, group, label, overlay: {load, style}, query(point, seq), render(result) })`. `group` is one of `political | safety | schools | geography`. Overlays lazy-load their boundaries on first toggle and are cached; `query` runs locally against the cached geometry. Families of similar layers are built by factory helpers (`registerPolygonLayer`, `registerSchoolZone`, `registerCpsNetwork`, `registerIlgaChamber`, `registerNearestPointLayer`) — follow the existing factory when adding a sibling. The factories derive each layer's hover-popup identity (`hoverName`) from the same properties its card reads, so the two surfaces can't disagree; a bespoke `registerLayer` block declares `hoverName(feature)` explicitly. Optional contract field `pointOfInterest(result) => {label, address} | null` drops a geocoded map pin (used by the school-zone layers). Optional contract field `coverage(point) => boolean | Promise<boolean>` declares where a layer's data applies (location relevance): outside its coverage the layer *hides* — toggle block, card, map overlay, hover-snapshot row, relationship outlines, and query all suppressed — without touching `state.layersOn`, so `layers=` permalinks survive and the layer reappears when a selected point re-enters coverage; a throwing coverage test fails open (the layer runs as if undeclared). Chicago's city-scoped layers declare `chicagoCoverage` (ERSB tiling), the Cook-scoped layers `cookCountyCoverage` (CCBR tiling), and the Will/DuPage/Lake/Kane/McHenry/Kendall county layers `willCountyCoverage`/`dupageCountyCoverage`/`lakeCountyCoverage`/`kaneCountyCoverage`/`mchenryCountyCoverage`/`kendallCountyCoverage` (pre-built county outlines); layers that declare nothing behave exactly as before the field existed. Cross-county concept layers (`county-board`, `judicial-subcircuit`, `fire-district`, `park-district`, `library-district`, `county-precinct`) register through the fork-level `registerCountyLayer` dispatcher — one toggle holding a per-county entry table, coverage = the OR of its counties' tests; polygon-style entries reuse `polygonCountyEntry`, and adding a county to a shipped concept is a dispatch entry, not a new layer (`docs/EXPANSION_GUIDE.md` Part 2). An entry's coverage can be narrower than its county: `county-precinct`'s Cook entry uses `suburbanCookCoverage` (in Cook AND NOT in Chicago) because city precincts belong to the separate `ward-precinct` layer. Two engine loaders exist purely for metro forks and are unused by Chicago's own layers: `loadArcGISPaged` (transfer-cap paging) and `makeSocrataPointLoader` (point datasets whose coordinates live only in properties) — see `docs/EXPANSION_GUIDE.md` Part 4.

**Two invariants that pervade the code:**

1. **Stale-async guard via `sequence`.** Every point selection bumps `state.sequence`. Async work captures `seq` and bails (`if (seq !== state.sequence) return;`) when a newer point has been selected. Preserve this in any code that awaits between selection and render.

2. **Per-layer failure isolation.** Each result card is independent: a layer whose data source is down shows an error + Retry *inside its own card* and never affects the others. Never let one layer's failure throw out of its `query`/`render` into shared code.

**Result-card content order (fleet convention):** a card leads with the layer name (the card header), then the district identifier, then — wherever a verifiable source exists — the representative(s)/officeholder(s), the office location, contact info (phone/email), and a link to more detail, in that order. Deviations are allowed where the concept demands them (nearest-N lists, layers with no elected officer), but when identity, location, or contact data exists in a layer's source, surface it on the card rather than leaving it in the dataset. New layers and card edits follow this order; known gaps are tracked in `docs/DATA_LAYER_GUIDEBOOK.md`'s backlog.

**Honesty rules (non-negotiable, enforced in review):** officeholder data is never guessed. Where no verifiable roster source exists, cards link to the official body instead of inventing a name. External strings are always sanitized or set via `textContent`.

## Cross-metro engine parity

This app is one of several sibling metro forks (NYC at `ThursdaysFamous/DistrictExplorer-NYC` / nyc.chidistricts.com; SF at `ThursdaysFamous/DistrictExplorer-SF` / sf.chidistricts.com); **Chicago is the reference implementation**. The metro-agnostic engine inside `index.html` is fenced with `/* ==== ENGINE:BEGIN <name> ==== */ … ENGINE:END` markers and must stay **byte-identical across forks**; everything city-specific those blocks reference lives in the `METRO:BEGIN config` block near the top of the script. When editing:

- Don't edit inside an ENGINE fence unless the change will be ported to every sibling fork — and port it as the **actual git diff**, never by re-describing the feature in a prompt (same prompt ≠ same code; that's exactly how the forks drifted before the fences existed).
- Region-agnostic changes land in this repo first; siblings apply the diff verbatim.
- Never inline a city-specific value in an ENGINE block — add a variable to the METRO config block instead.
- Verify with `python3 scripts/check_engine_parity.py index.html` (fence lint). **The engine RELEASE CHANNEL was retired in R2.1** (`docs/DEV_PROCESS_ASSESSMENT.md`): the fleet is consolidating into this one repo, so there is one copy of the engine and parity holds because there is nothing left to keep in sync. The deploy no longer splices a pinned bundle over the tree — the committed bytes are the deployed bytes, which also means a fence edit now actually reaches production instead of being overwritten at deploy time. Until NYC and SF are imported (R3) they simply stop receiving bumps.
- Full protocol + the known reconciliation backlog: `docs/ENGINE_SYNC.md`.

## Data pipeline

Most layers fetch live public APIs at runtime (Chicago Data Portal / Socrata, CPD ArcGIS, Cook County GIS, Census TIGERweb, Nominatim). Layers with **no public API** ship their data as same-origin files under `data/app/`, fetched on first toggle:

- **Boundary geometry** (`school-board-districts.json`, `il-supreme-court-districts.json`, `ccbr-districts.json`, the pre-built `{congress,il-senate,il-house}-districts.json` from TIGERweb via `scripts/build_legislative_boundaries.py`, and `will-county-outline.json` for the coverage test) — mapshaper-simplified from the full-precision GeoJSON in `data/` (originals in `data/source/raw/`). Regenerate with `scripts/build_embedded_boundaries.py` (rare operator step). Service worker serves these **cache-first** (boundaries change ~once a decade).
- **Officeholder rosters + curated lists** (`il-{senate,house}-members.json`, `congress-roster.json`, `cpd-district-info.json`, `ccpsa-district-councils.json`, `will-county-board-members.json`, `kane-county-board-members.json`, `lake-county-board-roles.json` — regenerated **weekly by CI** from scraper output; `kendall-county-board-members.json` + `mchenry-county-board-members.json` hand-verified (those counties block all automated fetch incl. the Internet Archive's crawler — their weekly workflows attempt an engine ladder and track the block on standing issues); `school-board-members.json` hand-curated; `early-voting-sites.json` hand-transcribed per election). Service worker serves these **network-first** so a returning visitor never gets a stale officeholder. Officeholder sourcing for any NEW county/metro layer is determined and built in the same change that ships the boundary (`docs/EXPANSION_GUIDE.md` Part 2 rule 4).

**Scraper → builder pattern** (each roster has a pair): a `*_scraper.py` produces intermediate JSON, a `build_*.py` writes the `data/app/*.json` file with count guards (it refuses to write if too few records resolve). `scripts/requirements.txt` pins deps; the CPD scraper additionally needs Playwright for a Cloudflare managed-challenge fetch. Builders emit plain JSON via `json.dump` into `data/app/`, and the app renders every external string through `sanitize()`/`textContent` — together that closes the injection surface for scraped text. (The historical `js_string()`-style `</script>` + U+2028/U+2029 escaping guard existed only when data was spliced directly into the HTML; it closed a real injection bug then, and becomes mandatory again only if scraped text is ever spliced into HTML/JS — don't.)

## CI workflows (`.github/workflows/`)

- `smoke-test.yml` — runs the behaviour gate on every PR and push to `main`, plus the static drift gates and **`check_roster_retention.py`**, which compares every roster field against the change's base and fails when one stops being published. It checks out with `fetch-depth: 0` for that reason. This is the gate that would have caught Brown's seven e-mails going silently empty; roster refreshes land here as bot PRs, which is exactly where it needs to fire.
- `update-{ilga,congress,cpd,ccpsa,will-county-board,ccbr,county-clerk}-roster.yml` — weekly (staggered) roster refreshes. Each re-scrapes, rebuilds `data/app/`, runs `validate_index.py`, and — if anything changed — **opens a PR rather than committing to `main`.** Officeholder data always gets a human review before it ships. Match this pattern for any new roster: never auto-commit roster changes to `main`.
- **`BOT_PR_TOKEN` (repo secret) — why every roster PR runs CI.** A PR opened with the default `GITHUB_TOKEN` fires **no** workflows: GitHub suppresses recursive triggers, so the smoke test never ran on a bot PR and the PR page showed zero checks. All 13 PR-opening workflows now check out and open their PR as `BOT_PR_TOKEN` instead. It needs Contents **read/write** (push the `bot/*` branch) + Pull requests **read/write** (open it) on this repo — the same shape as `ENGINE_DISPATCH_TOKEN`. Every reference is `${{ secrets.BOT_PR_TOKEN || github.token }}`, so an absent or expired secret degrades to the old behaviour (a PR with no checks) rather than breaking the refresh — which also means **a silently expired PAT looks exactly like the bug it fixed**: if bot PRs stop showing checks, check the token before anything else. Data is still gated either way, because each workflow runs `validate_index.py` on the rebuilt files before opening the PR; the PAT is what makes that visible on the PR itself.
- `validate-sources.yml` — monthly source-freshness check. Runs `scripts/validate_sources.py` **and `scripts/validate_card_links.py`**, folding both reports into one issue; on any WARN/FAIL (e.g. a Socrata dataset superseded by a newer-year edition, or a shapefile source gone unreachable) it **opens or updates a single tracking issue** rather than editing anything — the job stays green, the issue is the signal. Same "surface for a human, don't auto-apply" convention as the roster PRs. The issue title is matched to update in place, so it stays `Layer source drift detected — review needed` even though it now carries link findings too — renaming it would orphan the open issue.
- `fleet-status.yml` — weekly (Mon 15:00 UTC) fleet aggregator, **Chicago repo only**: `scripts/fleet_status.py` reads `metros.json`, checks every fork's deploy/engine-pin/roster state, diffs each fork's layer roster against `docs/DATA_LAYER_GUIDEBOOK.md`'s coverage map, and diffs each fork's shipped `data/app/coverage-gaps.json` against the guidebook's gaps block. That last one is the **only** gate on sibling gap drift: the guidebook lives here, so a sibling's file is generated here and lands there via a bump PR — it has no local `--check`. It also diffs every fork's `docs/ENGINE_SYNC.md` against CHI's and checks that file's ENGINE block inventory against the fences actually present in `index.html`/`sw.js` — the file that says "the SAME copy ships in every fork" had drifted 164 lines with an inventory naming 50 blocks against 53 real fences, because `check_engine_parity.py` compares fenced CODE and nothing compared the document. Doc drift is reported in BOTH directions: a fork holding lines CHI lacks says "reconcile both ways", never "overwrite". Reports on a single standing issue.
- Engine release machinery — **RETIRED in R2.1** (`release-engine.yml`, `create-engine-tag.yml`, `engine-parity.yml`, `update-state-template.yml` deleted along with `apply_engine.py`, `build_engine_artifact.py`, `build_state_template.py`, `bootstrap_state.py`, `check_template_placeholders.py`, `templates/state/` and `engine.lock.json`). One repo, one copy of the engine, no fan-out.
- `deploy-pages.yml` — publishes `main` to GitHub Pages (chidistricts.com).

## Conventions

- Code style is ES5-flavored (`var`, `function` expressions) throughout `index.html` — match it when editing existing modules.
- The "verified" date shown in the UI lives in `metro-worksheet.json` as `verified_date`; bump it there and regenerate when reverifying data sources. It used to be hand-written near the boot block in `index.html`, which was fine while one surface printed it — `sources.html` prints it too, and two hand-kept copies of one date is how they drift, so both are now GENERATED regions off that one key.
- This is a public-facing civic tool that explicitly disclaims legal precision — accuracy and the honesty rules matter more than feature velocity.
