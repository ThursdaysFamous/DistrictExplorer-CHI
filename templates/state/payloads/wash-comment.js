  // Out-of-scope wash: a light grey wash over everything beyond this fork's
  // coverage, drawn at idle from data/app/metro-outline.json — for a state
  // fork, the state outline bootstrap_state.py dissolves from TIGERweb at
  // bootstrap. It marks where coverage genuinely ends (the cards already hide
  // an out-of-coverage layer outright), and it skips silently on any error —
  // it is a hint, not data; regenerate the file, never hand-edit it.
