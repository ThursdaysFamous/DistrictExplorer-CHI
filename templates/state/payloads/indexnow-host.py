# STATE-TEMPLATE SCAFFOLD — bootstrap_state.py fills HOST from --domain and
# generates a fresh KEY (the <KEY>.txt ownership file must be served at the
# site root before this script is first run).
HOST = "newstate.example"
KEY = "00000000000000000000000000000000"